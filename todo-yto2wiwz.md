# Project TODO

- [x] Publish verified revision `39686522` to the managed Living Nexus platform.
- [x] Validate checkpoint `6e630442` (source revision `39686522`): `/`, `/explore`, `/manifest`, and `/manage` returned HTTP 200 on `livingnexus.manus.space`.
- [x] Synchronize the latest loop-release revision and inspect the registration migration plus affected schema.
- [x] Apply `drizzle/00xx_loop_registration_columns.sql` safely and verify the new registration columns exist without altering existing works.
- [x] Run the required release checks and publish the Loop release to the managed platform (typecheck and 406-test suite passed; checkpoint `fe76d568` published).
- [x] Validate public Register, work-detail, creator, and management route availability; record the deployed version (`fe76d568`).
- [x] Merge approved PR #2 (`cursor/loop-music-provenance-redesign-0e97` at `eab094a2`) into `main` as `9d87a917`.
- [x] Verify the registration migration preserves existing songs and applies default participation and visual values (9 columns present; 0 legacy rows missing defaults).
- [x] Hand off the authenticated Draft-registration, WID tone/waveform, and visual/profile publish-gate checks to the user at their request; no production work was created by the agent.
- [x] Validate a pre-Loop public work (`/song/1`) and creator profile (`/creator/1`): both load with the WID chain, participation defaults, 5W profile, and testimony section.
- [x] Confirm the live logged-out Register route presents the Loop registration entry point and sign-in handoff.
- [x] Review PR #3 for the batch Draft/Published toggle and Loop domain hierarchy scope: clean merge state, five focused files, and no schema or migration changes.
- [x] Merge approved PR #3 (`cursor/loop-post-release-followups-0e97`) into `main` as `0af48d55`.
- [x] Run release checks and publish the post-release follow-up revision (typecheck passed; 39 test files and 406 tests passed; checkpoint `8d8adfd3` published).
- [x] Validate public routes and the Loop creator page; verify in the merged implementation that batch intent defaults to Draft and propagates status, while non-music shelves remain preserved but hidden.
- [x] Hand off the authenticated Batch Upload toggle and rendered domain-layout smoke test to the user’s signed-in checklist; no account-bound production action was performed by the agent.
- [x] Restore the interrupted archive export repair: Archiver 8 compatibility, structured export failures, client-side ZIP response validation, and route-level regression coverage (41 files / 413 tests passing).
- [x] Save a dedicated checkpoint for the restored archive export repair before starting the PNA pull-request sequence (`e8709fed`).
- [x] Merge PR #4 after review confirmed its eight-file PNA route/presence scope and no schema or migration impact (`74e3e252`).
- [x] Review and merge PR #6 (theme remapping and PNA diary) after PR #4 is safely on `main` (`0c17049f`).
- [x] Correct PR #6’s diary schema and migration to use the production table’s existing camelCase base columns before adding the diary fields; apply and verify the additive production migration (9 columns and 2 indexes).
- [x] Rebase and reconcile PR #6 with merged PR #4 while preserving both the PNA route foundation and theme/diary changes (`1b63dbaf`).
- [x] Review and merge PR #5 (Guide slots) after PR #6 is safely on `main` (`7a8ec581`).
- [x] Apply and verify PR #5’s additive Guide slots and growth-signal migration before exposing its create and purchase flows (owner has 2 used / 3 total slots; growth and purchase tables present; no creator exceeds their limit).
- [x] Reconcile PR #5 with the merged PNA and diary releases while preserving guide-slot limits, growth signals, and the restored stewarded routes (`116010d8`).
- [x] Run the release validation suite for the ordered PNA sequence: exact conflict-marker scan clean, typecheck passed, 41 files / 413 tests passed, primary route responses returned HTTP 200, and the local production build completed successfully.
- [x] Publish the residual merge-marker recovery after verifying the repaired management page builds in production.
- [x] Review the theme persistence and cream-mode typography branch: clean and mergeable PR #7 with no schema migration; theme ownership is centralized and core text is remapped to theme-aware tokens.
- [x] Merge `cursor/theme-persist-typography-0e97` into `main` as `77ab9b07` after review.
- [x] Validate the theme contract in code: named-theme selectors are present, ThemeProvider is the sole runtime writer, and Parchment Cream maps to espresso ink on a cream surface.
- [x] Visually validate all four themes on core Loop and chrome surfaces in an isolated live-browser context; persisted captures confirmed distinct dark schemes and a readable Parchment Cream management surface.
- [x] Exercise theme persistence across reloads: Cathedral Dark, Crimson (with legacy Lights On set), Illuminated Gold, and Parchment Cream each restored the selected scheme without a Lights Mode overwrite.
- [x] Repair the live-browser Parchment Cream reload fallback to Cathedral Dark by preventing server Lights Mode sync from overriding an explicit saved theme.
- [x] Repair remaining low-contrast pale heading and supporting-text overrides on the live Parchment Cream management surface by reasserting cream tokens after the late default identity palette.
- [x] Replace the remaining hard-coded pale Home hero heading with the theme-aware parchment token; audit confirms core Manage, Work, Creator, TopBar, and LeftRail text uses theme-aware tokens or decorative non-text colors.
- [x] Visually verify all four persisted themes on Home, Manage, Loop Work, Loop Creator, TopBar, and LeftRail after the token cleanup through the isolated browser matrix and saved captures.
- [x] Review PR #8 (`cursor/pna-diary-browser-0e97`) for Keeper NOTES diary browsing, WID-CNV sealing, and PNA thread-restoration compatibility: no migration; protected owner-scoped archive APIs enforce access and sealing is idempotent.
- [x] Merge PR #8 after the repaired theme foundation is preserved (`1eb4f5a2`).
- [x] Validate the Keeper NOTES Notes | Diaries browser non-destructively: protected owner-scoped backend procedures, client state handling, and PNA handoff were verified; the signed-in list/detail/seal smoke remains user-owned and no archive record was altered by the agent.
- [x] Add focused regression coverage for write-once and consume-once PNA diary restoration handoff state (2 tests passed).
- [x] Verify unauthenticated Keeper archive calls return structured API errors rather than HTML fallback responses (HTTP 401, `application/json`, `UNAUTHORIZED`).
- [x] Add regression coverage for theme normalization, persistence, and light-versus-dark document application (3 focused adapter tests passed).
- [x] Run release checks for the validated theme repair: typecheck passed, 42 test files / 416 tests passed, production build passed, and the multi-route four-theme browser matrix passed.
- [x] Save a dedicated checkpoint for the validated theme repair before merging PR #8 (`69793f0f`).
- [x] Review PR #10 (`cursor/pna-chat-workspace-0e97`) for chat-workspace integration and provider hierarchy impact: PlayerProvider is lifted around WorkEditorProvider, covering both CreativeDrawer and PNA with a single player context.
- [x] Repair the `CreativeDrawer` `usePlayer` crash by restoring a valid PlayerProvider ownership boundary in the reviewed PR #10 composition.
- [x] Trace the reported PNA 503 and distinguish it from a code-path regression: the supplied report contains no target request URL; it is separate from the provider crash and cannot be reproduced or attributed from the available evidence.
- [x] Merge PR #10 PNA chat workspace and PlayerProvider repair into `main` (`a97c98ca`).
- [x] Add regression coverage for the repaired PNA workspace provider composition (2 assertions verify PlayerProvider encloses WorkEditorProvider and is not duplicated inside App).
- [x] Run PNA workspace validation: typecheck passed, 44 test files / 420 tests passed, production build passed, and the reported 503 remains unlinked to a reproducible request.
- [x] Confirm a route-specific PNA workspace element renders in an isolated browser: `/pna` reached the Provenance Nexus Avatar sign-in gate with no ErrorBoundary or PlayerProvider error.
- [x] Locate the Home-page render path that mixes `background` with `backgroundImage` across rerenders: TopBar layered `background` with an optional harmonic `backgroundImage`.
- [x] Replace the conflicting shorthand with `backgroundColor`, update the transition property, and add a focused TopBar style regression assertion.
- [x] Validate the Home render path and publish readiness: focused TopBar test passed, isolated Home rerender assertion found no background style conflict, full regression suite passed, and production build passed.
- [x] Trace the Explore maximum update depth loop to render-phase `useAuth` localStorage writes, which can force external-store rerenders in authenticated route chrome.
- [x] Stabilize the subscription boundary by moving runtime user synchronization into an idempotent post-commit effect and add focused regression coverage.
- [x] Move `useAuth` runtime user-info persistence and session marking out of render into an idempotent effect.
- [x] Validate Explore rendering in an isolated browser with no maximum-depth error; focused test, full regression suite, and production build passed.
- [x] Validate authenticated `/explore` after the `useAuth` persistence repair: an isolated signed sandbox session loaded the owner user record, rendered the Explore heading, and emitted neither a maximum-update-depth error nor an error boundary.
- [x] Add and run a working route-level authenticated Explore regression harness that uses the project session SDK, checks persisted user state after commit, captures runtime errors, and clears its sandbox cookie afterward.
- [x] Review PR #12 (`cursor/pna-unify-legible-0e97`) as the preferred replacement for PR #11: it excludes the floating Keeper chat from `/pna`, preserves the avatar-registry surface, and keeps Keeper NOTES handoff consume-once.
- [x] Verify PNA mode, NOTES, track-stage, and scholar presentation share the site theme tokens: local chat-skin storage and mode rainbow colors are removed; modes and chat use `--ln-gold`, `--ln-parchment`, `--ln-smoke`, and panel tokens.
- [x] Merge and publish PR #12 (`0861061e`) after confirming compatibility with the current workspace repair (published checkpoint `92a27be9`); combined route, theme, and authenticated Explore validation completed.
- [x] Validate PR #12 non-destructively: the `/pna` sign-in workspace gate renders without an error boundary, unified-chat regression coverage passes, and the four-theme browser matrix passes across Home, Manage, Work, Creator, TopBar, and LeftRail.
- [x] Review PR #13 for Surface Map alignment, public redirect safety, and compatibility with the PNA and Explore releases: refreshed branch `8dcfa908` is cleanly mergeable, migration-free, and retains the unified PNA routing and Explore repair foundations.
- [x] Verify Explore exposes music and creator discovery only while marketplace and store redirect to Avatar Registry and TopBar Provenance routes safely through OAuth to PNA.
- [x] Review the rebuilt Home orientation flow: the isolated route matrix confirmed the Porch content and guest orientation, while source review confirmed loading-aware PNA/Register CTA paths and limited showcase support flow.
- [x] Merge PR #13 (`96e4069d`) after the resolved Home review; regression, route validation, and publication remain pending.
- [x] Directly verify PR #13’s resolved `HomePage` retains the orientation porch with current authenticated state handling and theme-safe typography.
- [x] Confirm the resolved Home source uses theme-safe `font-display`, `font-heading`, and `font-body` classes without hardcoded families and exposes loading-aware `ENTER PNA` / `SIGN IN · ENTER PNA` labels before merge.
- [x] Verify Home orientation, Explore insulation, and canonical redirect behavior after the resolved PR is merged through the isolated browser matrix.
- [x] Restore the guest TopBar Provenance OAuth handoff so external sign-in uses browser navigation with `/pna` as its return path.
- [x] Restore actual Explore insulation by hiding the shared marketplace drawer and removing the remaining PNA store CTA.
- [x] Add a project-owned rendered-route browser integration test that clicks guest Provenance navigation and asserts OAuth returns to `/pna` while Explore excludes marketplace, store, skins, and guides content.
- [x] Re-run the project-owned browser integration test and Porch matrix after the durable test is in place; the CDP test and final matrix both passed against preview, default regression completed with 49 files / 427 tests passing (one preview-only browser test skipped without a target), and production build passed.
- [x] Trace the duplicate `/keeper` entry in the PNA navigation source that produces non-unique React keys: `QUICK_ACTIONS` defined both Notes & Diaries and Keeper with the same href.
- [x] Deduplicate the PNA navigation model while preserving the intended Keeper/NOTES entry point and add regression coverage.
- [x] Verify the PNA workspace emits no duplicate-key warning in the isolated route validator; focused PNA tests, full regression suite, and production build passed.
- [x] Retain the `Notes & Diaries` Keeper entry and remove the duplicate `/keeper` navigation row from `PNAShellPage`.
- [x] Review Draft PR #14 for final Porch, Explore, marketplace-chrome, and surface-contract insulation compatibility: mergeable against `c9dd8b37`, migration-free, and consistently isolates PNA Store commerce from Loop chrome while narrowing Explore to audio discovery.
- [x] Verify Home Capture copy is track-specific and Explore is songs-and-artists-only without multi-medium content: focused contracts pass; live Porch matrix reports `homePorch: true` and `exploreMusicAndCreatorsOnly: true`.
- [x] Verify Marketplace drawer removal from Loop chrome and canonical Avatar Registry redirects: focused contracts pass; live Porch matrix resolves `/marketplace` and `/store` to `/avatar-registry`.
- [x] Merge PR #14 (`ec35f031`) after review and complete validation: typecheck plus focused contracts passed; the live Porch matrix passed; 50 files / 428 tests passed with one browser test intentionally skipped without its target; production build passed.
- [x] Review Draft PR #16 (`AUTHORIZED_AGENT_DOCTRINE.md` and Loop spec §10 pointer): ontology, authority, Bridge billing, non-silent seal/publish, admin attribution, and Agent Ledger align with the foundation. One correction is required before promotion: replace “Domain Continuity … revocable” with access revocable while recorded knowledge remains restricted or superseded, never erased, per Law V Chain immutability. No agentic code implemented.
- [x] Merge Law-V-clean PR #16 (`04b66ffd`) into GitHub main after verifying the explicit Law-V access-restriction and supersession language.
- [x] Synchronize merged PR #16 into the managed platform and checkpoint the governed foundation (`e03a5805`): the local doctrine preserves Law-V continuity truth and the Loop §10 pointer is present.
- [x] Implement the first agentic foundation slice only: creator-scoped Commission → Capability authority → append-only Agent Ledger for music Draft work. Added ADR-016, three isolated additive tables, protected `agents` procedures, canonical hashed ledger evidence, and focused tests; excluded publishing, sealing, external Bridges, off-platform reasoning, and multi-agent behavior.
- [x] Validate authorization, owner scoping, append-only ledger evidence, Draft-only music boundaries, and regression/build readiness: migration applied and schema/indexes verified; focused contract/router tests and `pnpm check` passed; controlled full regression and production build completed successfully.
- [x] Review PR #17 (`work-page-three-column`): the triptych correctly separates full Testimony, Chain of Record/WID, and Creator actions; hero copy is capped at 140 characters; drawer ontology labels are correct. Hold promotion for two narrow corrections: Creator share must use canonical `/creator/${creator.id}` rather than a possibly unsupported handle path, and new hardcoded accent/fallback colors must be replaced by Living Nexus theme tokens.
- [x] Review PR #18: all existing CTA references are centralized on `https://discord.gg/GfyPrjzU6`; the widget has a descriptive title and restrictive sandbox; Welcome and Attribution use the shared constant; no invite/server-setting change is attempted. Hold promotion for one correction: replace new Home Porch hardcoded Discord/accent/fallback colors with established Living Nexus theme tokens.
- [x] Review updated PR #18: `https://discord.gg/dqCmTY5Ucb` is correctly centralized through the shared constant across TopBar, mobile header, Left Rail, Home CTA, Welcome, Attribution, and widget context; links have accessible labels and the widget remains sandboxed. Hold promotion: Home, TopBar, mobile header, and Left Rail now add further hardcoded Discord colors, so the required theme-token correction remains incomplete and has expanded; extract the repeated Discord glyph while correcting the token usage.
- [x] Diagnose reported failure of `https://discord.gg/dqCmTY5Ucb` from Home/navigation: root cause was the published site serving the pre-merge `ADF9dtVA` source while PR #18 was still unmerged. Merged and published the canonical invite, tokenized the affected Home/TopBar/mobile/Left Rail presentation, consolidated active glyph use, verified the live Home’s Left Rail, TopBar, and JOIN DISCORD destinations, and passed the focused contract, TypeScript, 53 files / 436 tests (one browser test skipped), and production build.
- [x] Diagnose and repair the mobile Discord-widget’s endless loading state: the configured Discord widget itself remains indefinitely loading when opened directly, so the source is provider-side rather than Living Nexus layout. Replaced the iframe with a bounded native Community panel that keeps both direct canonical actions, uses theme tokens and the shared glyph, and cannot render a perpetual spinner. Preview inspection, focused contract/typecheck, controlled full regression (54 files / 440 tests with one browser test skipped), and production build passed.
- [x] Produce the Cartography Directive deliverable before further implementation: created the source-indexed documentation package under `docs/cartography/` covering current code truth, doctrine, ethics, registry, Living Object, creator rights, AI transparency, claims, competitive systems, architecture, findings, and phased roadmap. Findings only; no product behavior was modified.
- [x] Apply the Legal Review Boundary throughout the Cartography Directive: the README, audit charter, claim registry, rights map, and AI map separately classify Doctrine, Technical Fact, Creator Declaration, Evidence, and Legal Position; legal conclusions are marked unresolved / qualified counsel required and neither doctrine nor law was rewritten to force alignment.
- [x] Cancel Cartography publication work at the Keeper’s direction: `/docs/cartography/` remains intentionally unmounted; no documentation route, access model, password mechanism, or public exposure will be built. Existing Cartography files remain internal historical records only.
- [x] Conduct a wide access-boundary search before any password work: inventoried actual router/docs-subdomain behavior, OAuth session cookies, protected/admin tRPC controls, user roles, owner-only draft/export access, API-key hashing, policy surfaces, and the absence of account-password auth. Recorded the evidence and five access options in `docs/cartography/ACCESS_BOUNDARY_FINDING.md`; no authentication or publication code was added.
- [x] Review PR #19 Breath Canon: the slow opacity/transform-only direction, Home HarmonicContext use, text contrast, and no-blur intent align. Hold promotion for four corrections: Work must source hue and playing state from the current global HarmonicContext (not the displayed song signature plus any-track `isPlaying`); define or remove the referenced `art-breathe` animation; resolve `--ln-gold-hot` through the formal theme authority rather than fallback-only use; and disable edge transitions/cover-hover transforms under reduced motion. The Cartography access-boundary hold remains separate.
- [x] Diagnose and repair `/explore` maximum-update-depth error reported 2026-08-13: traced authenticated route pressure to `useAuth` persisting an equivalent tRPC user object by unstable reference, causing repeat storage/session side effects under heavy Explore observers. Replaced the effect dependency with a stable serialized identity/version payload, retained idempotent storage comparison, added focused regression coverage, verified guest Explore render, passed `pnpm check`, controlled full regression, and production build.
- [x] Review PR #20 Creator Sanctuary: layered DOM/WID posture, public-playlist filtering, witness count, and owner/witness language are directionally aligned. Hold promotion for four corrections: `/creator/${handle}/playlists` has no route and must be removed or added; narrow touch devices must not run the permanent parallax rAF/touch listeners and reduced-motion must suppress hover transitions; replace new hardcoded sanctuary colors with `--ln-*` tokens; and add focused route, quality-tier, public-playlist, and WID-filter regression coverage. The Cartography access decision remains separate.
- [x] Review PR #21: Work-page CTA exposure, track metadata propagation, `songs.download` server enforcement, checkout-return retry, public-playlist DB filtering, and no-blur direction are sound bases. Hold promotion for six corrections: make tip-download checkout authenticated server-side (it is currently a `publicProcedure`, so UI-only login can create unlinked purchases); replace the `$1.79` fallback with an explicit creator/server price rather than claiming $2.00; validate checkout origin server-side; remove or route the inherited `/creator/${handle}/playlists` dead link; correct inherited touch/reduced-motion/token debt in Sanctuary/Depth layers; and replace source-string assertions with behavioral entitlement, player metadata, public playlist, route, and tier tests.
- [x] Re-review combined PR #21 as the sole proposed integration path: GitHub still reports the identical previously reviewed head `3f2680b4d8bccf566ddeb07148f1c034a87c9bb9` and no automated check results, so the claimed six blocker corrections are not present for verification. Hold merge; PR #20 remains unmerged.
- [x] Re-review latest PR #21 (`2ffa7934`) as the sole combined integration path: music-native blocks, audio filtering, 12-plus-show-all, domain hydration, owner-only provenance, and public album selection are materially present; `/album/:collectionWid` is mounted. Hold promotion because the new head still retains `publicProcedure` checkout, `$1.79` fallback and client-controlled origin, dead `/creator/${handle}/playlists` fallbacks, hardcoded/new fallback colors, touch/reduced-motion debt, and source-string rather than behavioral tests. WID-ALB is displayed through existing `collectionWid` data but no new issuance/immutability proof is present in this PR.
- [x] Produce a dual-environment Link and Checkout Integrity map: recorded repository route/procedure facts separately from managed database and public-runtime observations in `docs/cartography/PRODUCTION_EQUIVALENT_INTEGRATION_AUDIT.md` and `INTEGRATION_AUDIT_MATRIX.md`. Reconciled public route, WID, storage, migration, registration, batch, payment, playlist, and active-surface deltas; user-owned authenticated, payment, webhook, and mutation flows remain explicitly runtime-unverified rather than inferred.
- [x] Conduct a read-only production-equivalent integration audit: compared sanitized live schema/migration metadata, WID/collection/playlist/tone/waveform aggregates, public routes, storage headers, source contracts, managed logs, and a 243-work Creator surface. Published pass–fail matrix, sanitized API shapes, HTTP status evidence, blocker classes, and an authorized sandbox-validation sequence; no data, configuration, payment, webhook, credentials, or user-owned state was changed.
- [x] Produce a source-verified Dacr Inc. corporate and product-history brief: resolved active Florida entity DACR INC.; documented officer/founder-evidence limits, 2025 corporate/web/app/public-unveiling chronology, no public funding disclosure located, scoped USPTO exact-name search results, and the 1999-domain versus 2025-product distinction. Delivered cited brief, entity card, and source ledger under `docs/research/dacr/`; preserved unresolved legal and IP conclusions.
- [x] Package the completed Dacr research into a standalone, readable dossier file with its chronology, evidence matrix, source links, funding/IP boundaries, and unresolved legal questions: created and diff-validated `docs/research/dacr/DACR_INC_EVIDENCE_DOSSIER.md` without changing the underlying research conclusions.
- [x] Produce a source-bound Dacr-versus-Living Nexus comparison map: created and diff-validated `docs/research/dacr/DACR_VS_LIVING_NEXUS_COMPARISON_MAP.md`, comparing evidence layers, creator authority, provenance, registry, commerce, AI/agents, governance, and preservation while preserving legal-claim, technical-verification, and doctrine-versus-implementation boundaries.

- [x] Restore image generation as a first-class PNA Creator Studio capability before implementation: completed the five-thread direct-source audit and delivered `docs/ADR-026-PNA-VISUAL-COMPOSER.md`. The design restores image generation within active PNA—not the orphaned legacy route—with a model-agnostic Visual Composer, up to four creator-approved references with roles/hashes, source-labeled prompt provenance, optional non-diagnostic intended emotional-arc reflection, private Quiver-first custody, typed Draft attachment, separate confirmations for all consequential actions, additive model/migration plan, six-layer alignment, test gates, and rollback. No runtime behavior changed; implementation awaits Keeper approval.
- [x] Make the PNA Visual Composer mobile-first before implementation: updated `docs/ADR-026-PNA-VISUAL-COMPOSER.md` with the scoped two-finger pinch-to-zoom/pan preview contract, preserved native browser accessibility zoom, one-handable full-width actions, no-hover/keyboard-equivalent rules, `dvh` viewport, safe-area/bottom-stack, portal/fixed panel, scroll-lock cleanup, reduced-motion, iOS/Android validation, risk, and rollback requirements. No runtime interaction changed; implementation awaits Keeper approval.
- [x] Map and design the conversion of authored Personal Nexus avatars into free Avatar Shop models: verified real Guide and Avatar Registry schemas/routes plus inventory readiness; defined separate Guide-source and AVT-representation records, zero-price attributable claims, authored narrative/rights snapshots, and no Agent-authority escalation. Delivered `docs/ADR-022-GUIDE-AVATAR-SHOP-CONVERSION.md`; implementation is blocked on Keeper selection, derivative-permission completion, strengths label, existing-skin compatibility, and 2D-versus-3D scope decisions.
- [x] Produce an intentional Nexus Context Panel architecture review: verified the active navigation drawer, Explore feed, Work Living Context Rail, PNA workspace/diary/player seams, and host boundaries; delivered and diff-validated `docs/ADR-023-NEXUS-CONTEXT-PANEL.md` with canonical context references, authority/privacy rules, front-end/back-end/data contracts, mobile/performance limits, failure modes, test gates, rollback, deliberate phases, and Keeper decisions before any extraction or Agent behavior implementation.
- [x] Implement Keeper-approved ADR-023 Phase 1 only: added strict typed Work/Creator/Provenance/Explore/Now Playing references, explicit source-labelled suggestions, resolver/action contracts, a PNA-only Context Canvas, session-local state, click-to-open Now Playing context, and Open/Verify/user-initiated Play actions. Excluded persistence, diary integration, auto-switching, commerce, registry mutation, and secondary hosts. Added five behavioral tests; `pnpm check`, controlled full regression (55 files / 442 tests, 1 browser test skipped), production build, and `pnpm refine` completed. Guest PNA route reaches the normal sign-in gate; authenticated Canvas interaction remains a user-owned smoke check.
- [x] Produce a creator-workflow product reset map before further feature promotion: delivered and diff-validated `docs/CREATOR_WORKFLOW_RESET_MAP.md`, reconciling what is live, gated, drafted, and documentation-only; mapping actual avatar/Guide/shop data, music upload/metadata/WID capabilities, and PNA context limits; and defining a single Creator Nexus workflow, safe AI sandbox, Avatar choose/equip/change journey, P0–P5 sequence, and mandatory five-field release proof before new feature work.
- [x] Conduct a non-mutating Living Nexus 0.1 forensic inventory: delivered and diff-validated `docs/LIVING_NEXUS_0_1_FORENSIC_INVENTORY.md` from direct active source/routes plus sanitized aggregate database evidence. It confirms the image service, S3 storage chain, 86 Quiver records, Guide/AVT data, and marketplace substrate survive; identifies `/keeper-compose` as an orphaned Studio path, legacy visual-work routing as intentionally delisted, and one missing Creator Studio composition surface; defines evidence-first recovery phases without mutating assets, records, listings, entitlements, or routes.
- [x] Assess the submitted Local-First Creative Workspace & Public Nexus directive before implementation: completed a direct-source audit of workspace/ingestion/registry/WID/provenance/Quiver/Guide/avatar/agent/storage/deployment seams and published `docs/ADR-024-LOCAL-FIRST-CREATIVE-WORKSPACE.md` plus directive intake record. The ADR retains public music-only Explore, inventories reusable foundations/gaps, compares native-local and browser-offline options, maps the smallest local music vertical slice, and documents authority, risk, rollback, test, and approval gates. No code, schema, storage, production record, or publication behavior changed.
- [x] Define the Local ↔ Public database boundary for the proposed architecture: ADR-024 establishes separate authority scopes, a consent-controlled application sync boundary, outbox/inbox event records, local/public projections, idempotency, conflict queues, append-only provenance posture, allowed data directions, and offline/retry requirements. Implementation remains blocked pending Keeper approval.
- [x] Design the model-agnostic PNA Ingestion Commission before implementation: delivered and diff-validated `docs/ADR-025-MODEL-AGNOSTIC-PNA-INGESTION-COMMISSION.md`. It defines local/hosted model adapters, permission-scoped registry/Studio retrieval tools, deterministic file/hash/WID/provenance services, bounded Commission states, single-use confirmation artifacts, additive operational records, private-Draft first slice, local-model follow-on, authority boundaries, test gates, and rollback. No runtime behavior changed; implementation awaits Keeper approval.
- [x] Reconcile merged GitHub main PRs #17/#19/#21 with the managed Living Nexus deployment: verified GitHub `main` at `4ed9a0a1`, confirmed PR #17/#19/#21 merge lineage and the managed-public deployment gap, and wrote `docs/RELEASE_RECONCILIATION_2026-08-14.md`. The actual merged source still has unauthenticated tip-download checkout attribution, a $1.79 fallback, client-controlled checkout origin, creator-playlist dead links, and source-string rather than behavioral release tests. No platform publish, payment, user record, or configuration change was made.
- [x] Record the reconciled release decision: checkpoint `509a53c7` already published merged GitHub main version `707c6f87`, so the Loop visual release is live. The Keeper owns the hard-refresh smoke of Home, one Work, one Creator, and Explore; no more review-only checkpoints or cross-scope holds will be created on `main`.
- [x] Authorized after Keeper live smoke on `707c6f87`: implemented only the payment lock. Tip-download checkout is now a `protectedProcedure`; its input accepts only `https://www.livingnexus.org`; Stripe metadata always carries the authenticated user; redirect URLs are server-owned; and focused behavioral tests prove guest denial plus noncanonical-origin rejection. `pnpm check`, focused contract tests (8 assertions), full regression (57 files / 450 tests passing, 1 browser test skipped), `pnpm refine`, and diff hygiene completed. No price, playlist, theme, motion, architecture, PNA, or other feature change was mixed into the patch.
- [x] Record the Keeper-approved PNA image-workflow decision without implementation: updated ADR-026 to establish one primary Visual Composer in the creator interface, one creator-private Quiver shelf for every generated/reference/remix/cover-candidate/attached image asset, model-neutral adapters beneath, and the canonical Compose → Quiver → asset-detail workflow. A generation Commission groups variants and evidence but never creates a second shelf; `/keeper-compose` remains retired. No runtime behavior changed.
- [x] Trace the reported legacy Composer regression before implementation: verified the working `KeeperComposePage` still contains prompt/reference generation and private Quiver-save paths, but `/keeper-compose` now redirects to `/pna`; PNA’s `handleSend` routes every prompt to text-only `keeper.chat`/`invokeLLM`, which has no visual-intent tool, reference handoff, generator invocation, structured artifact response, or Quiver-save confirmation. The smallest safe restoration is a Vision/Compose chat action that prepares a `generate_private_visual` proposal, calls the preserved generation service, returns a generation card, and asks separately before invoking the existing private `quiver.save` path. No runtime behavior changed.
- [x] Implement the Keeper-authorized first PNA cover-art generation slice only: Vision-mode plain-language prompts in both active PNA chat surfaces now call the preserved protected generator, produce a private image result card, and require an explicit “Save to Quiver privately” click before the existing creator-owned Quiver save mutation writes a record. Added a shared mobile-safe card, switched the side-panel Generate Artwork shortcut into Vision, added protected-creator/guest-denial/confirmation-contract coverage, passed `pnpm check`, focused tests, full regression (58 passed / 1 skipped files; 453 passed / 1 skipped tests), `pnpm refine` (baseline score retained), diff hygiene, and mobile guest-gate inspection. Excluded reference uploads, remix, Draft attachment, WID registration, sync, publication, model pickers, and unrelated PNA changes.
- [x] Repair the reported PNA provider-generation failure without expanding feature scope: selected the supported server-side `MODEL_GPT_IMAGE_2` explicitly for PNA artwork rather than allowing the failing Nano Banana default; extended the internal wrapper’s model contract; converted provider errors into a creator-safe no-save response; and added focused protected-primary-model, guest-denial, provider-failure/no-storage, and explicit-Quiver-confirmation coverage. `pnpm check`, focused tests, full regression (58 passed / 1 skipped files; 454 passed / 1 skipped tests), `pnpm refine` baseline, and diff hygiene passed.
- [ ] User-owned live PNA image-provider smoke after the repair: in Vision mode, submit one non-sensitive cover-art prompt; confirm a private result card appears, then separately select Save to Quiver privately. Report the exact result/error; no agent-created creator artwork is needed for this validation.
- [x] Repair the observed five-minute PNA image-generation hang: added a 45-second server-side abort deadline to the image provider wrapper; timeouts return a creator-safe error and PNA’s existing `finally` clears the pending state for retry; focused tests prove a stalled request is aborted without storage writes, retains model selection, and does not reach Quiver. `pnpm check`, focused tests, full regression (59 passed / 1 skipped files; 456 passed / 1 skipped tests), `pnpm refine` baseline, and diff hygiene passed. No asset custody, registration, or publication behavior changed.
- [x] Record the Keeper’s Draft PR #22 hold: PR #22 remains unmerged; its PNA chat, bounded-motion, now-playing, in-place USE/UNLOCK, and chrome changes are excluded from the active release path. Authenticated desktop/mobile rendered-interface smoke for that draft remains user-owned and does not authorize a merge.
- [x] Save the current managed Living Nexus state for the Keeper to republish: created checkpoint `c13a8d15`, preserving the published 45-second PNA image timeout repair and the Draft PR #22 hold. No draft code was merged and the user-owned authenticated smoke remains unchanged.
- [x] Record the Keeper’s Draft PR #23 hold: PR #23 remains unmerged alongside PR #22. Its responsive PNA drawer correction is gated on user-owned authenticated phone and desktop smoke—hamburger to rail icon opens the panel, backdrop dismisses it, links navigate, and route changes close the rail/drawer. No merge is authorized by source checks alone.
- [ ] User-owned authenticated Draft PR #23 responsive-shell and ADR-027 smoke: verify the revised desktop three-region shell starts with its in-flow sidebar open and persistent through navigation; rail toggles the class-based context drawer; mobile hamburger overlay still slides correctly; player strip stays in the main column; and `#global-player`, `#context-drawer`, `#chat`, message roles, and PNA sidebar context hooks are present. Then play a sealed track and confirm the strip, drawer, and PNA chat display its existing WID; play an unsealed track and confirm “Unsealed · no WID” with no invented lineage or chat-originated sealing. PR #23 and #22 remain unmerged regardless of source checks.
- [x] Repair the reported PNA Vision prompt-limit error: both active PNA inputs now show a Vision-only live `0 / 1,000` counter, retain all typed content without silent truncation, disable over-limit generation, announce invalid state accessibly, and return a concise “draft is still here” correction message rather than raw `too_big` JSON. The protected router rejects 1,001 characters before the generator/storage path; focused boundary/translation and PNA behavior tests passed. Full regression passed (60 passed / 1 skipped files; 459 passed / 1 skipped tests), `pnpm refine` baseline retained, and diff hygiene passed. Provider selection, timeout, asset custody, Draft PR status, registration, and publication behavior remain unchanged.
- [x] Reconcile the reported public-delivery mismatch before requesting any further user smoke: verified managed checkpoint/source `90965956`, queried all three public domains, and fetched the live lazy-loaded `PNAShellPage-C14xjhrW.js` route chunk directly. It contains the Vision prompt-limit correction, private proposal, and Quiver-save markers, so delivery is current; the user-visible empty result is isolated to provider availability, not cache/checkpoint/draft delivery. Draft PR #22/#23 smoke remains halted unless explicitly promoted.
- [x] Repair the confirmed live PNA image-provider availability failure before any further user smoke: queried the protected internal catalog and verified both available models; direct non-persistent health probes showed `MODEL_GPT_IMAGE_2` timed out with zero bytes after 55 seconds while `MODEL_GEMINI_2_5_FLASH_IMAGE_PREVIEW` returned HTTP 200 in 5.3 seconds. PNA now selects the proven Gemini model through the same model-neutral adapter; protected/private/explicit-Quiver boundaries are unchanged. `pnpm check`, focused provider/PNA tests (7), full regression (60 passed / 1 skipped files; 459 passed / 1 skipped tests), refinement baseline, and diff hygiene passed. No creator image, S3 object, or Quiver record was created during health validation; Draft PR #22/#23 remain unmerged.
- [x] Map and design the coherent PNA creator-workspace recovery before implementation: completed a five-seam source audit plus primary durable-conversation/structured-output research and delivered `docs/ADR-028-PNA-CREATOR-WORKSPACE-CONTINUITY.md`. The ADR unifies durable owner-scoped threads, fork-safe diary reopening, visible private Quiver shelf/detail routes, generated-asset continuity, model-neutral structured image reflection/revision proposals, Working State versus Chain-of-Record barriers, mobile workspace coordination, additive models/migrations, risks, tests, rollback, and recommends Slice A—continuity and reachability—before interpretation or Draft attachment. Runtime behavior is unchanged pending Keeper approval.
- [x] Implement Keeper-authorized ADR-028 Slice A only: added and applied the strictly additive `pna_threads` and `pna_thread_messages` migration; implemented protected owner-scoped thread create/list/get/append/proposal procedures and private Quiver detail access; wired full PNA to route-backed durable thread restoration, New Thread, a reachable private Quiver shelf/detail view, post-save return to Quiver, and a safe back path to the thread. Diary archives remain immutable and separate; image result confirmation, provider, Working State labels, WID/publication boundaries, reflection, remix, Draft attachment, sync, and Draft PR #22/#23 remain untouched. Schema presence verified; focused contract tests, full regression (61 passed / 1 skipped files; 461 passed / 1 skipped tests), final typecheck, refinement baseline, diff hygiene, and unauthenticated desktop/mobile entry checks completed.

- [x] Build the submitted Spatial Registry Mock as an isolated, fictional-data prototype route: implemented `/prototype/spatial-registry` with the supplied fictional “Yahweh Lights My Way” / Weave & Breathe / `LN-00017` fixture, a real Three.js node-and-line registry, Profile → Work → Edit → Register → Witness → Lineage → Player interaction, simulated registration and witness events, registration capacity presentation, canonical in-environment mock player, Cover Art Studio mock, My AI mock, desktop/mobile visual verification, and an explicit registry-is-not-visualization boundary. Added a direct prototype-isolation test; `pnpm check`, focused test, full regression (62 passed / 1 skipped files; 463 passed / 1 skipped tests), refinement baseline, and diff hygiene passed. No production data, auth, storage, WIDs, witnesses, payments, marketplace, blockchain, or full agent system is connected.

- [x] Repair the live creator-blocking Draft/Publish `charCodeAt` failure: traced the actual failure to `MusicEnvironment.handlePublish`, where an incomplete/restored `tone.accents` entry reached `a.charCodeAt(1)` while assembling the shared Draft/Publish upload payload. Added `harmonicSignatureFromAccents()` in the shared Loop registration contract; it preserves valid deterministic signatures and produces safe three-slot fallbacks for missing/non-string accents. Both Draft and Publish now use that helper. Focused regression covers valid, incomplete, and absent accents; TypeScript, full regression (62 passed / 1 skipped files; 465 passed / 1 skipped tests), refinement baseline (69), and diff hygiene passed. No existing song, WID, provenance, or publication record was modified.

- [ ] Create the non-destructive Core–Doctrine Pack–Next transition blueprint before any repository split or rollback: inventory retained doctrine, registry/music upload/metadata/WID contracts, historical known-good registration capabilities, and retention rules; define a versioned Doctrine Pack and Core read contract for Cursor’s spatial Next engine; require retain/replace/migrate/archive decisions before any surface retirement. Correct Quiver vocabulary: Quiver is the private reserve of prepared creative assets; an Arrow is a selected asset; Fletching is refinement; Draw is deliberate named-work selection; Release is creator-confirmed outward use. No repository, production source, data, domain, or deployment action is authorized during this planning pass.
- [x] Create the non-destructive Core–Doctrine Pack–Next transition blueprint before any repository split or rollback: completed the five-stream inventory, read-only version archaeology, and `docs/ADR-029-CORE-DOCTRINE-NEXT-CONTINUITY.md`. The record defines Core, Doctrine Pack, and Next responsibilities; retains music registration/metadata/WID/provenance/creator authority; corrects Quiver vocabulary; identifies `eab094a2` and `ef69e102` as isolated recovery-comparison candidates; defines CoreProjection.v1, retain/replace/migrate/archive gates, backup posture, risks, six-layer alignment, and the only three Keeper decisions required before any repository, production, domain, or data action. Documentation-only validation passed: diff hygiene and `pnpm check`.

- [x] Conduct the Core ingestion and Registry-route integrity audit before any cleanup: traced single upload, batch upload, Draft save, Publish, metadata extraction, artwork, lyrics, tone/waveform/WID handoff, worker/queue responsibilities, persistence, and active/orphaned routes. Repaired confirmed source defects only, classified retained/replace/migrate paths with rollback posture, and delivered `docs/CORE_INGESTION_AND_REGISTRY_INTEGRITY_REPORT_2026-08-17.md`. No source, record, WID, provenance, or route deletion occurred. Delivery-manifest reconciliation and scheduler-dispatch activation remain separately tracked.

- [x] Repair the confirmed public `/batch-upload` failure: moved the unauthenticated gate after all hook declarations, ending the live React #300 hook-order crash. Added Core regression coverage and verified the development preview renders the sign-in gate without writing any batch, song, WID, asset, or creator data.

- [ ] Reconcile the post-checkpoint production delivery mismatch: after checkpoints `5a2b6308` and `8cec2bd2` reported published, all public hosts still served old `index-XaSH9KYH.js` and React #300. The latest backend cache headers are live, but requesting the new `BatchUploadPage-CKKZfV7d.js` returns the old HTML fallback (`text/html`) rather than a JavaScript asset, proving the published static filesystem/manifest did not receive the current frontend build. This is a managed deployment artifact mismatch, not an unresolved batch-component defect.

- [x] Repair confirmed SPA entry-document cache behavior: added explicit no-store/revalidate policy to `index.html` and SPA fallbacks only, retaining normal cache behavior for versioned assets. The policy is live, but it exposed the deeper managed static-artifact mismatch separately tracked above: the public static filesystem still contains the old entry document and lacks the current lazy chunk.

- [x] Unify confirmed Register navigation divergence: LeftRail Register and TopBar Register Work now route directly to canonical `/manifest` with `/manifest` login return paths. The batch route remains retained as a subordinate registration surface; Core route-contract regression coverage passes.

- [x] Repair the confirmed single-upload lyrics-provenance gap: canonical `songs.upload` now produces and persists the same owner-bound WID-LYR and lyrics hash as batch registration when lyrics are supplied. Existing works are untouched; focused parity coverage passes.

- [ ] Activate the durable LN visual worker after managed scheduler authentication is reconciled: project job `ln-visual-queue` (`RpNKCo7c9uZyiAJmV77GcV`) was created at one-minute cadence, but its first callback returned platform-level HTTP 403 `permission error for cron cookie` before the source handler response shape. The task is intentionally paused to prevent repeat failures; retain the task UID and resume only after the public deployment/runtime accepts the managed cron identity. No song, WID, artwork, or user-owned record was created or altered.

- [x] Produce a non-mutating Current-to-Target ontology and dependency map before any further implementation: completed six independent source passes across schema/data, identity, registration/provenance, routes, contexts/components, and API/Core doctrine. Delivered `docs/CURRENT_TO_TARGET_NEXUS_ONTOLOGY_MAP.md`, including current and target Mermaid maps, authority and dependency tables, page graph, target mapping, seams, six-layer alignment, and non-change conclusions. No runtime code, data, schema, route, asset, deployment, or configuration was changed.

- [ ] Hold all architecture and implementation work pending Keeper review of `docs/CURRENT_TO_TARGET_NEXUS_ONTOLOGY_MAP.md`. Do not create a Nexus Contract ADR, alter authority boundaries, move code, modify schema, change routes, or publish any functional update until explicit follow-on direction is provided.

- [x] Produce a non-mutating application-boundary refactor plan from `docs/CURRENT_TO_TARGET_NEXUS_ONTOLOGY_MAP.md`: completed six independent boundary passes across shell, Creator/Domain, Work, Registry, Explore/Player, and AI Workspace; delivered `docs/PROPOSED_APPLICATION_BOUNDARY_REFACTOR_PLAN.md` with target boundary authority, current asset classification, coupling matrix, allowed dependency direction, phased retain/extract/adapt/migrate/archive proposal, invariants, rollback/test gates, and Keeper decisions. No code, schema, route, data, deployment, configuration, or runtime behavior changed.

- [x] Produce a source-bound boundary-interface ledger from the current Living Nexus implementation: completed seven independent interface inventories and delivered `docs/EXISTING_BOUNDARY_INTERFACE_LEDGER.md`. The ledger records current caller/owner, route/context/tRPC procedure, observed input/output shape, persistence source, auth/ownership condition, confirmation/authority rule, forbidden transfer, and unverified evidence areas for Nexus Shell, Creator/Domain, Work, Registry/Provenance, Explore, Player Runtime, and AI Workspace. No code, schema, route, data, deployment, configuration, or runtime behavior changed.

- [x] Inspect the configured Living Nexus API integration metadata without reading, printing, changing, or exposing its secret value. Confirmed that a masked `OpenAI` API connector exists but is **disabled**; its declared OpenAI API capability includes text, code, image, and audio generation/analysis, streaming, structured output, and multimodal requests. Current Living Nexus model calls instead use `BUILT_IN_FORGE_API_URL` / `BUILT_IN_FORGE_API_KEY` through `server/_core/llm.ts`, explicitly selecting `gemini-2.5-flash`. No secret, configuration, code, data, deployment, or runtime behavior changed.

- [x] Apply the Keeper’s `LIVING NEXUS — ONTOLOGY ENFORCEMENT / ARCHITECTURAL REFACTOR` directive as a non-destructive preparation package: completed an exact `songsRouter` procedure index plus eight independent source passes across registration, Explore, Player/commerce, collection/project/domain, AI/social/creator, Nexus Shell, WorkSurface, and protected provenance seams. Delivered `docs/ONTOLOGY_ENFORCEMENT_REFACTOR_PREPARATION.md` with every current `songsRouter` procedure classified by current role/target domain/safe-move status/blocker, App/MainLayout and WorkSurface maps, protected files, provenance/data risks, safe migration order, and a single approval-gated `checkDuplicate` helper extraction. No application, schema, route, data, asset, WID, provenance, deployment, or configuration change was made.

- [x] Implement Keeper-approved **Slice 1 only**: extracted the read-only body of `songsRouter.checkDuplicate` to `server/domains/registry/lookupExistingWorkByFileHash.ts`, retaining the identical protected façade, Zod `fileHash` input, exact duplicate/no-match/own-match/other-creator response contract, and `ctx.user.id` requester source. Added `server/tests/workDuplicateLookup.test.ts` for 5 focused behavior/authorization/no-side-effect assertions. Passed focused tests, `pnpm check`, full regression (65 files passed, 1 skipped; 475 tests passed, 1 skipped), `pnpm refine` baseline (69/100, no API-consistency finding), and guest non-mutating `/manifest` plus `/batch-upload` smokes. Read-only custody query confirmed zero songs, WIDs, provenanceEvents, and quiverImages created at or after Slice 1 start. No schema, route, WID, provenance, upload, batch upload, Draft/Publish, storage, Player, payment, or public route behavior changed. Slice 2 is not authorized.

- [x] Produce a source-bound, non-mutating Current → Target Work Preparation / Registration / Publication map: completed six parallel lifecycle inventories plus direct active-source verification of MusicEnvironment, `songs.upload`/`batchUpload`, `updateStatus`/metadata/lyrics/credits/audio replacement, WID/provenance routers, Work schema, storage, and version routes. Delivered `docs/WORK_PREPARATION_REGISTRATION_PUBLICATION_CURRENT_TO_TARGET.md`, including all 13 requested current-path answers, timing map, explicit Registry/provenance distinction, edit effects, version semantics, risk observations, and a single non-implemented pure `PreparedWorkRegistration` snapshot candidate. No code, schema, data, WID, provenance, storage, route, or Slice 2 change was made; stop after report.

- [x] Implement Keeper-approved **Slice 2 only**: added pure `shared/preparedWorkRegistration.ts` and integrated it at the existing MusicEnvironment preparation/seal point. The model captures current local values, exposes informational WID-bound/editorial/independent-component classes only, retains byte-identical ordered WID JSON serialization, and rebuilds the prior `songs.upload` payload with identical values and key order. `generateWID()`, the ECDSA signing call, existing tRPC input, Draft/Published intent, asset upload, title/WID, lyrics WID, artwork, audio version, post-registration edits, provenance, schema, storage, Player, payment, and routes remain unchanged. `server/tests/preparedWorkRegistration.test.ts` passed 4 focused assertions, including WID bytes and full payload equivalence. `pnpm check` passed; full regression passed (66 files, 479 tests; 1 skipped each); `pnpm refine` retained 69/100 baseline/API-consistency pass; guest `/manifest` and `/batch-upload` smokes passed. Read-only custody check since Slice 2 start found 0 songs, 0 WIDs, 0 provenanceEvents, and 0 quiverImages. Slice 3 is not authorized.

- [x] Produce the Keeper-authorized **Slice 3 Work Revision Processing architecture report only**: completed eight-stream inspection, discarded inconsistent parallel findings, and verified the active Edit Chapel/WorkEditorContext, CreativeDrawer mutation callers, songs/db mutation paths, WID/provenance routers, dedicated versions router, schema, registration/seal boundary, and current projection directly from source. Delivered `docs/SLICE_3_WORK_REVISION_PROCESSING_ARCHITECTURE.md` with current architecture, schema, provenance/version capabilities, requested component classification, exact WID-bound distinction, revision ontology, human/AI boundary, projection model, holes, reuse/new infrastructure decision, smallest pure Slice 4 candidate, likely/protected files, risks, proof requirements, and rollback. No code, schema, data, WID, provenance, route, storage, or behavior changed; Slice 4 is not implemented.

- [x] Run a read-only discovery diagnostic for `WID-MUS-382C1FB8-0ACBA81A` / **ARMOR OF LIGHT (EXTENDED)**: completed six parallel streams, direct live database verification, public WID/tRPC/guest-route probes, and exact active-source tracing for canonical Work, Explore base, New This Week, Trending, Limited Showcase, creator projection, archive, Related Work, right-rail Registry, ranking/limits, and cache windows. Delivered `docs/ARMOR_OF_LIGHT_DISCOVERY_DIAGNOSIS.md`. Confirmed a current discovery/public-visibility consistency defect: the Work is `Published` but `isPublic = 0`; Explore/Home/creator/guest Work/related queries exclude it before ranking, while the WID Protocol and right-rail Witness Registry include it through a separate `Published + witnessId` path. No code, schema, data, cache, configuration, publication state, route, storage, WID, or provenance change was made; stopped after diagnosis.

- [x] Run a read-only publication-state semantics diagnostic for `WID-MUS-382C1FB8-0ACBA81A` / **ARMOR OF LIGHT (EXTENDED)**: completed six parallel traces, discarded unsupported/hallucinated path claims, and verified active schema/migration, registration, `updateStatus`, deletion, dismissal, moderation/API, metadata, CreativeDrawer, public/owner/WID/Registry readers, and live state combinations directly. Delivered `docs/PUBLICATION_STATE_SEMANTICS_DIAGNOSIS.md`. Confirmed that `Published + isPublic = false` is an invalid but currently reachable state—not a supported private-publication posture—because ordinary registration/status paths and migration 0074 enforce `isPublic = (status === "Published")`, while `updateMetadata` can update `status` without synchronizing `isPublic` and current editor UI calls that path. Live data contains two divergent rows. No code, schema, data, visibility, publication state, WID, provenance, storage, route, cache, deployment, or configuration change was made; stopped after diagnosis.

- [x] Implement Keeper-authorized **Publication State Integrity Repair only**: removed `status` from ordinary `songs.updateMetadata → updateSongMetadata` input/persistence, added `isPublicForSongStatus` inside unchanged owner-gated `updateSongStatus`, and routed changed lifecycle selection in both CreativeDrawer and EditChapel through existing `songs.updateStatus` only after metadata succeeds. Publication gates and ownership remain in dedicated status authority. Added `server/tests/songs.publicationStateIntegrity.test.ts`: Draft→Published, Published→Unlisted, Unlisted→Published, Draft metadata, Published metadata, status rejection through metadata, and full lifecycle invariant (7 tests; with existing status suite 10 focused tests). `pnpm check` passed; full suite passed (67 files / 486 tests, 1 skipped each); `pnpm refine` retained 69/100 baseline/API-consistency pass; guest `/manifest` and `/explore` smokes passed; owner authority is covered by tRPC contract tests while authenticated browser mutation smoke remains creator-owned. Read-only custody/state verification confirmed 0 new songs/WIDs/provenanceEvents/Quiver images and that both pre-existing `Published/isPublic=false` rows remain untouched. No schema, WID, provenance, storage, Player, payment, route, Registry predicate, or database-row repair occurred. Stop after checkpoint/report; no Slice 4 work.

- [x] Implement Keeper-authorized **Song Publication Fix only**: added pure `server/domains/work/publicationReadiness.ts` shared by direct Published registration and existing owner-gated status authority. Both now require and return one explicit creator-actionable list for full rights/license, bound visual, name/handle, bio/origin, profile photo, and testimony; direct Published registration now has parity with later Draft→Published on `ownershipStatus = partial`. WID, schema, Registry/provenance, storage, Explore design/predicates, existing records, and Revision Processing remain unchanged. Added `server/tests/songPublicationReadiness.test.ts` (5 tests) plus existing 10 status/invariant focused tests; `pnpm check` passed; full regression passed (68 files / 491 tests, 1 skipped each); `pnpm refine` retained 69/100 baseline/API-consistency pass; guest `/manifest` and `/explore` smokes passed. Read-only custody found 0 new Works/WIDs/provenanceEvents/Quiver images. The pre-slice audit found two divergent Published/false rows; final read-only check found one remaining pre-existing row (`WID-MUS-33032195-5E5D9AF8`, Waves | Waves), while ARMOR OF LIGHT was changed outside this slice. No existing Work was repaired here. Stop after checkpoint/report; no Slice 4 work.

- [x] Close the confirmed direct-registration publication-gate mismatch within the authorized Song Publication Fix: direct `songs.upload` Published creation and `updateSongStatus` now use the same pure readiness evaluator and the identical actionable creator message; partial ownership, cover, identity, bio/origin, photo, and testimony parity is covered by focused tests. No schema/table/status, WID/provenance/storage/Explore/Registry predicate, existing-row repair, or client mutation-contract change occurred.

- [x] Diagnose and repair only the production Billing lazy-asset incident: completed six parallel streams then reconciled conflicts with direct live HTTP, deployed module-graph, service-worker, browser route, console, static-delivery, Vite/build, and project registration evidence. Delivered `docs/BILLING_DYNAMIC_ASSET_403_DIAGNOSIS.md`. The reported exact chunk now returns HTTP 200 JavaScript on all three production domains, current deployed index bundle imports it directly, `/settings/billing` renders normally, and console is clean. Determined a transient deployment/cache incident with service-worker stale-pairing as plausible historical mechanism, but no live missing asset, permission, route, WAF, manifest mismatch, or reproducible fault. No speculative repair was made; no Billing logic, database, schema, WID, provenance, route, publication, static config, CDN, or service-worker configuration changed. Stop after checkpoint/report.

- [ ] Monitor the resolved Billing dynamic-asset incident only: the exact reported chunk and route are currently healthy. Do not modify Billing, static assets, service-worker, cache policy, deployment, CDN/proxy/WAF, or configuration unless the 403 recurs with fresh failing URL, timestamp, response headers/body, active worker/cache state, and deployed index bundle evidence.

- [x] Produce an architecture and smallest staged implementation plan for a protected `/admin` Control Plane before making changes: inspect and reuse existing server-side admin authorization, admin routes/pages, database access, audit/logging, diagnostics, feature flags, theme/design configuration, and public route status infrastructure. Define authenticated/authorized/validated/auditable/rollback-capable Preview → Apply configuration controls backed only by safe database values. Explicitly forbid arbitrary code/database/schema/WID/provenance/source editing and preserve all public Work, Registry, WID, provenance, publication, and route semantics. No code, schema, configuration, data, or runtime change until Keeper approval of the plan.

- [x] Correct the focused pure-readiness side-effect assertion within the authorized Song Publication Fix: changed the test to allow the legitimate `ownershipStatus` input name while proving the evaluator has no asynchronous, database, storage, WID, provenance-event, or status-transition call. No runtime publication behavior changed.

- [x] Complete the confirmed Edit Chapel parity repair within the authorized publication-state slice: TypeScript identified the remaining status-through-metadata caller in `client/src/components/EditChapel.tsx`; converted its owner save flow to persist ordinary metadata first and invoke existing `songs.updateStatus` only when the selected lifecycle value differs. Cover upload stays metadata-only; WID, provenance, storage, Player, payment, routes, publication gates, and existing rows remain untouched.

- [x] Implement Keeper-approved Admin Control Plane Stage 1 only: replace the root `/admin` landing with a server-authorized, read-only Control Plane shell that provides bounded system health, fixed canonical route status, diagnostics links, and a paginated audit-log view through a narrow `adminControlPlane` namespace. Reuse `adminProcedure`, existing dashboard patterns, existing read-only health/worker/audit infrastructure, and Living Nexus tokens. Preserve existing specialized `/admin/*` routes. Do not add settings/design/feature-flag/Preview→Apply mutations; arbitrary code/database/schema/WID/provenance/source editing; publication/Registry/payment/role controls; arbitrary route targets; or any public Work, WID, provenance, publication, Registry, payment, schema, storage, or configuration behavior change. Added `server/tests/adminControlPlane.test.ts` covering guest/non-admin denial, authorized bounded read-only response shapes, audit-detail redaction, absence of mutation procedures/integrations, root route/shell contract, and server-owned manifest wording. Passed focused 4/4, `pnpm check`, full regression (69 files / 495 tests passed; 1 browser file / 1 test skipped), `pnpm refine` baseline/API-consistency pass, local no-session server probe (HTTP 403 before readers), fixed route HTTP probes (five 200s), production build, and diff hygiene. Authenticated rendered admin smoke remains Keeper-owned; no further Stage 2–4 capability is authorized.

- [x] Implement Keeper-approved Admin Control Plane Stage 2 only: extended the protected `/admin` Control Plane with observe-understand-navigate-only system overview counts from verified existing tables (creator identities, active works, public Published works, Draft works, WIDs, provenance events, collections), bounded redacted recent administrator activity, grouped fixed route inventory (Public, Creator, Work, Registry, Admin), explicit health/instrumentation limits, diagnostics links, Event/Actor/Timestamp/derived Area/derived Result audit columns, existing specialized `/admin/*` directory, and token-consistent operational identity. Storage/build status are accurately labeled `not-instrumented`; worker is accurately labeled `reachability-only`. Preserved specialized routes. Added aggregate/health/navigation tests; focused suite passed 5/5, `pnpm check`, full regression (69 files / 496 tests passed; 1 browser file / 1 test skipped), `pnpm refine` baseline 69/API consistency pass, no-session aggregate endpoint probe (HTTP 403), production build, and diff hygiene passed. Desktop public capture reaches the existing intro gate; authenticated rendered Control Plane smoke remains Keeper-owned. No settings/design/feature-flag/Preview→Apply/page-control mutations, arbitrary code/database/schema/WID/provenance/source editing, publication/Registry/payment/role controls, arbitrary URL checking, automatic repair, persistence, or any public Work/WID/provenance/publication/Registry/payment/storage/configuration behavior change. Stop after Stage 2.

- [ ] Diagnose the empty-log managed deployment failure reported immediately after Stage 2 checkpoint `c5dc666a`: collect deployment/runtime/build/dependency/static-artifact evidence and determine whether the blocker is code/packaging versus managed-platform infrastructure. Repair only a source-proven deploy cause if present; preserve the completed Stage 2 read-only Control Plane boundary and do not expand its capabilities. Revalidate deploy readiness, checkpoint only a verified repair, and report cause, evidence, live-state impact, and rollback.

- [x] Restart the Living Nexus managed development server only, per Keeper order. Stopped all email/support-thread activity and made no source, schema, database, deployment, or Control Plane change. Verified local readiness: server listening at `http://localhost:3000/`, database pool initialized, visual-queue recovery initialized, and existing worker schedules resumed. The managed preview URL was not yet returned by the restart service; local service is healthy.

- [ ] Produce a read-only comparison of realistic commercial hosting alternatives for Living Nexus as a full-stack platform with server, database, authentication, object storage, Stripe, durable/periodic worker requirements, and custom domain. Distinguish managed deployment options from migration-heavy infrastructure paths, cite current official sources, state compatibility/operational implications, and do not change project code, configuration, data, domains, deployment, or hosting.

- [x] Verify the recovered managed Living Nexus publication read-only: all three public domains return HTTP 200 and now serve `/assets/index-fl28fZZA.js` rather than the prior `index-DPaGLPKc.js` bundle. The public new main bundle references `AdminControlPlanePage-DJsKpw1_.js`, and that Stage 2 lazy chunk returns HTTP 200. Recovery/promotion is verified. No project source, configuration, data, deployment, or Control Plane behavior changed during verification.

- [x] Implement the Keeper-approved `dark-gold-vault.mp4` entrance video enhancement: prepared the supplied six-second 16:9 video as durable webdev asset `/manus-storage/dark-gold-vault_1238ee74.mp4`; added it only as a layered, non-clickable entrance background beneath the existing Living Nexus CinematicSplash; preserved readable foreground hierarchy, keyboard/Skip Intro access, centered mobile crop, token-based veil, autoplay/muted/playsInline/loop/metadata-preload safeguards, and a reduced-motion path that loads neither video nor particles while retaining the existing void/glow ceremony. Added focused contract tests. Desktop and 390px mobile visual verification passed. Focused suite 3/3, `pnpm check`, full suite (70 files / 499 tests passed; 1 skipped), refinement baseline/API consistency, production build, and diff hygiene passed. No controls/audio/new interactive surface/design-system or Work/WID/provenance/Registry/publication/payment/route semantic change. Stop after checkpoint.

- [x] Repair the reported `dark-gold-vault` CinematicSplash regression: the live screenshot showed the vault film excessively occluded and the splash typography/layout visibly contracted or shifted. Corrected only video/veil opacity/luminance/layering plus existing splash typography/layout: film now uses full opacity with bounded source luminance and a restrained edge/top-bottom scrim; title, card title/body, waveform, labels, and archive entry now use larger bounded token-backed hierarchy; local card contrast replaces global occlusion. Preserved non-interactivity, mute/loop/inline behavior, reduced-motion fallback, Skip Intro, keyboard/manual process controls, tokens, and all non-splash semantics. Expanded the focused contract to 4 assertions; `pnpm check`, full regression (70 files / 500 tests passed; 1 skipped), refinement baseline/API consistency, diff hygiene, production build, and 1440px/390px visual verification passed. Checkpoint and stop.

- [x] Recompose the `dark-gold-vault` CinematicSplash to the Keeper-approved second-reference image: vault-first animated entry with a clear centered corridor; compact seal separate from Skip Intro (no overlap); title/subtitle and waveform in the upper field; no process card competing during the first visual reveal; manual process cards only after deliberate entry/advance. Preserve the existing decorative/non-interactive/reduced-motion/session/Skip/keyboard safety rules and all product semantics. Capture and self-review desktop/mobile screenshots against the reference, present them to the Keeper, and do not publish until explicit visual approval.

- [x] Upgrade only the CinematicSplash foreground against the Keeper’s mobile reference: move Skip Intro to a true safe-area-aware top-right placement on mobile; tighten mobile process-card density; apply a token-based transparent/glass center card with proper Cinzel/Cormorant/DM Sans hierarchy; add a restrained foreground gold flare/edge-light effect; and restyle every remaining process slide through the shared card system. Do not alter the video, background/veil, route, session, Work, WID, provenance, Registry, publication, payment, or other product behavior. Capture desktop/mobile reviews and do not publish until Keeper approval.

- [x] Complete the read-only PNA retirement and avatar-successor audit: mapped PNA’s routes, mutable private threads, optional sealed `WID-CNV-*` diary path, private Keeper portraits, marketplace skin/AVT catalog state, current equipment projection, Guides, explicit Quiver saves, and the legacy private image proposal flow. Measured aggregate preservation inventory without reading creator content; documented the recommended archive-window-first transition, route matrix, successor invariants, zero-data-mutation sequence, flags/rollback, acceptance gates, and the separate equipment-entitlement hardening concern in `docs/lnls/architecture/ADR-PNA-RETIREMENT-AND-AVATAR-SUCCESSOR.md`. No PNA code, route, schema, record, asset, WID, AVT identifier, entitlement, provider, or deployment was changed.
- [ ] Await Doc’s explicit selection and authorization before implementing any PNA archive/export UI, server-side read-only mode, route redirect, surface retirement, successor bridge, avatar/portrait migration, entitlement repair, record suppression, or deletion.
- [x] Diagnose the reported mismatch between the public Living Nexus entrance and the intended sandbox vault-first composition: compare the attached production reference, active uncheckpointed source, managed preview, and public bundle/asset state; identify whether the cause is phase timing, source drift, uncheckpointed work, cached/static delivery, or a foreground composition defect. Do not alter or publish code until the evidence and Keeper direction are established.
- [x] Add `VaultofGold.mp3` to the CinematicSplash as a durable private storage asset with resumable position, indefinite loop, audible-start attempt plus muted autoplay fallback, explicit mute/unmute control, adjustable 40% default volume, and cleanup on Skip/Archive exit. Preserve all splash visuals and product semantics; validate with focused contract tests, browser playback/resume/cleanup checks, full regression, typecheck, refinement, and production build before publication.
- [x] Make the centered CinematicSplash equalizer respond to the actual `VaultofGold.mp3` playback signal instead of decorative timing: use a browser-safe `AnalyserNode` path tied to the existing audio element, preserve the 40% volume/resume/loop/mute controls, use a deterministic fallback when analysis is unavailable, preserve reduced-motion behavior and the vault video/background, and validate reactivity plus cleanup before any publish.
- [x] Diagnose and repair the reported mobile CinematicSplash failure: VaultofGold is not audible after the user’s interaction and the centered waveform appears dead. Isolate persisted mute state, trusted gesture handling, storage-origin/Web Audio analysis, and mobile rendering; preserve video/background, layout, loop/resume controls, and all platform semantics; validate before publication.
- [x] Repair the published CinematicSplash equalizer synchronization so the center waveform visibly responds in real time to active `VaultofGold.mp3` playback, not merely to a decorative timer. Preserve audio playback, resume/loop/mute/volume behavior, the vault video/background, mobile layout, reduced-motion safeguards, and platform semantics; validate playback and visual response before publication.
- [x] Diagnose and repair the repeated sandbox and deployed CinematicSplash failure where VaultofGold appears muted or inaudible and the center waveform shows no visible real-time response. Compare both environments, isolate autoplay/mute/media/analyser state, preserve the vault video and splash composition, validate both environments, and republish only after proof.
- [x] Diagnose and repair the combined CinematicSplash media failure: the vault video loop is not visibly advancing and the centered waveform does not respond while VaultofGold is audible. Compare sandbox/deployed runtime state, isolate video playback, audio clock, analyser output, and canvas rendering independently, preserve the volume override and existing composition, validate motion before publication, and do not publish until confirmed.
- [x] Verify and repair CinematicSplash HTML5 video loop playback attributes and inspect browser-console/runtime `AudioContext → MediaElementSource → AnalyserNode → canvas` connections for the non-responsive waveform. Preserve the existing video asset, visual composition, audio controls, reduced-motion safeguards, and platform semantics; validate in sandbox and production before publication.
- [x] Repair the continued sandbox visual defect where the audio-reactive equalizer is technically active but visually reads as a dead line and the playback state is ambiguous. Preserve the analyser connection, user volume override, video loop, splash composition, and platform semantics; validate readable motion before publication.
- [x] Promote the validated CinematicSplash media repair so the centered real-time equalizer, HTML5 video loop hardening, and same-origin VaultofGold playback are visible on the public site; run final regression/build/worktree checks and verify the live bundle and media routes after publication.
- [x] Publish the approved centered real-time equalizer, HTML5 video loop hardening, and same-origin VaultofGold playback repair after final regression, refinement, production build, and live verification.
- [x] Refine only CinematicSplash foreground UI on mobile: keep cinematic video, VaultofGold playback, analyser-driven waveform, looping, flare/lighting, synchronization, and overall visual identity exactly unchanged; make the information card a tighter translucent glass layer, preserve content/onboarding sequence, improve typography/spacing/hierarchy/mobile-safe dimensions, and make Skip Intro smaller/cleaner within the top-right safe area. Validate protected media contracts and foreground visuals before publication.
- [x] Resume the bounded Home/Porch Best Played This Week slice: audit existing play records first; target top three eligible public tracks by rolling seven-day plays; each artifact has clickable work/track, creator, and Support actions; refine Home typography around featured rotation; do not add a worker or new persistence unless the audit proves it necessary.
- [x] Verify GitHub and managed-project state against `living-nexus-site-redesign-handoff.md`; inspect unfinished Codex work, current branch/PR/commit state, working-tree divergence, and checkpoint alignment; classify conflicts and safe continuation points without overwriting, merging, publishing, or changing source until separately authorized.
- [x] Verify whether any Living Nexus source changes beyond the last published checkpoint require deployment; compare local worktree, GitHub `main`, managed checkpoint/public bundle, and relevant connected remote state; classify deployable changes versus draft/held work without overwriting, merging, publishing, or changing source until separately authorized.
- [x] Add a read-only Home Best Played This Week projection: strict rolling-seven-day eligible public Published audio tracks, Top 3, clickable work/creator/Support actions, honest empty state, no worker or new persistence; preserve CinematicSplash and registry semantics.
- [x] Complete and validate CinematicSplash desktop diagnostics/static fallback styling, then run full regression and visual review before any checkpoint.
- [x] Fix CinematicSplash autoplay rejection so a browser policy failure does not force or persist mute; preserve explicit user mute preference, Play sound activation, waveform, analyser, volume, and cleanup behavior.
- [x] Replace CinematicSplash’s vault film with the user-provided `dark-gold-vault.mp4`, remove the rendered static image fallback on desktop/mobile, preserve reduced-motion safeguards and all audio/analyser/loop/skip behavior, validate both viewports, and stop for approval before checkpointing.
- [x] Repair the CinematicSplash first-click Play sound control so starting paused audio does not immediately toggle it muted; preserve intentional mute/unmute toggles, stored user preference, volume, waveform, analyser, and video behavior.
- [x] Repair desktop-only CinematicSplash vault-film startup so the working mobile video remains unchanged while desktop reliably presents the same first moving frame without a static-image fallback.
- [ ] Monitor and reconcile the managed public frontend-bundle promotion mismatch: checkpoint `daac3577` is saved, but all public domains currently serve prior `index-ArqsMUX1.js` without the approved vault preload or mute-v3 markers. Do not modify source, data, schema, or configuration unless a source-side cause is proven.
- [x] Gate CinematicSplash’s desktop vault/process reveal on the actual video `canplay` and resolved `play()` state, with a bounded error/reduced-motion void path, so the controls do not present over an unready dark film; preserve mobile behavior, the same video, and all audio/analyser systems.
- [x] Integrate the compatible Cinematic Sovereign Lineage and supported-fetch-preload assertions from GitHub commits `82337d29`, `8301e917`, and `72dbb1ae` without changing the actual vault video element or overwriting the approved cinematic work.
- [x] Under explicit Option 2 approval, remove only `docs/lnls/design/artifacts/living-nexus-home-art-direction-v1.png` and `docs/lnls/design/artifacts/living-nexus-web-app-art-direction-v1.png` from the deployable repository to unblock the cinematic checkpoint; preserve all design/lineage text, tokens, splash code, and desktop framing.
- [x] Tune the desktop vault-film crop to `scale(2.15)` only above 640px so the same documented 24fps/7.292-second forward vault loop is perceived comparably to the tighter mobile framing; preserve mobile, film asset, audio/analyser, loop, and foreground content.
- [x] Under explicit managed-resync authorization, restore published checkpoint `9e1cc4f5`, synchronize GitHub `main` through `72dbb1ae`, then reapply only the approved desktop `2.15×` vault crop and the two Option-2-authorized repository PNG deletions; validate before retrying the approved cinematic checkpoint.
- [x] Review GitHub commit `6d6e2554` for the warmer Parchment Cream token, swatch, pre-paint, and contract refinement as a separate release candidate; do not integrate or publish it until explicit release sequencing is approved alongside the current video-only branch conflict.
- [x] Hold cinematic-video and deployment-reconciliation work; proceed only with user-observed Living Nexus site changes, scoped one at a time from the live surface and visually reviewed before any publish.
- [ ] Reconcile and publish GitHub commit `6d6e2554` refining Parchment Cream to deeper parchment, espresso ink, layered cards, clean popovers, aged gold, and legible muted/border tokens; preserve the video-work hold and validate theme contracts, responsive rendering, and public delivery.
- [ ] Under explicit authorization, synchronize the managed project to GitHub `main` at `6d6e2554`, confirm the resulting managed SHA, and promote only the Parchment Cream theme refinement while holding cinematic-video work and leaving art-direction files untouched.
- [ ] Under explicit theme-only clean-commit authorization, preserve only the Parchment Cream source changes from `6d6e2554`; exclude the `4cb5ea3d` CinematicSplash reduced-motion changes and all art-direction files; synchronize, validate, and publish only the isolated theme refinement.
- [ ] Under explicit instruction, open the managed GitHub/version controls and resolve Living Nexus source to `3029067ec446eea96c536a9df259a7b9d830326f`; do not reapply or modify held cinematic-video work, then confirm the managed SHA.
- [ ] Perform a read-only inventory of current local workspace changes, managed base, GitHub main ancestry, and pending ledger items; classify release scope and risk without source modification, synchronization, merge, or publication.
- [ ] Reconcile the managed Living Nexus source from checkpoint `7777b669` to Keeper-approved GitHub commit `3029067ec446eea96c536a9df259a7b9d830326f`, preserving the Parchment Cream theme and the Explore surface while excluding held cinematic-video and unrelated art-direction changes.
- [ ] Validate the reconciled source with `pnpm check`, relevant theme/Explore Vitest contracts, and a production build; visually inspect critical responsive routes without publishing.
- [ ] Save a review-only checkpoint after validation and report its version to the Keeper; hold publication until explicit confirmation.
- [x] Apply the Keeper-authorized strict Parchment Cream repair from `6d6e2554` onto managed `7777b669` using only `client/index.html`, `client/src/components/ThemeSwitcher.tsx`, `client/src/index.css`, `client/src/lib/theme-dom.ts`, `server/tests/parchmentCreamTheme.contract.test.ts`, and `server/tests/themeDom.test.ts`; preserve Explore and exclude every CinematicSplash, cinematic-test, splash-route/proxy, and `server/_core/index.ts` change.
- [x] Validate the strict six-file Parchment Cream repair locally, report the clean diff/commit/tests/build, and do not checkpoint or publish because this project auto-publishes checkpoints. `pnpm check`, the two relevant test files (5 assertions), production build, diff hygiene, and source-scope checks passed. Rendered visual confirmation remains unclaimed because the temporary preview returned blank/unavailable captures despite local `/` and `/explore` HTTP 200 responses.
- [ ] Diagnose the existing held CinematicSplash contract inconsistency separately: a full-suite attempt from the managed `7777b669` baseline fails one pre-existing assertion for CSS markers `49.8%`/`50.2%` in `server/tests/cinematicSplash.videoContract.test.ts`; no CinematicSplash, cinematic-test, video, proxy, or Explore source was modified in the strict Parchment Cream repair. Do not resume this held lane without separate Keeper direction.
- [x] Create and push a clean GitHub branch/commit from managed `7777b669` containing only the six validated strict Parchment Cream source/test files, excluding the session ledger, visual-check notes, CinematicSplash, cinematic tests, splash/video proxy paths, `server/_core/index.ts`, and Explore; committed `dc85384198fb53c00ff93df371681568a22b0c9e` to `parchment-cream-strict-7777b669` and pushed it to `Whisperer217/living-nexus_provenance`. No pull request was created because the branch intentionally begins from the managed base rather than current GitHub `main`.
- [x] Under the Keeper’s explicit authorization, create the managed checkpoint that automatically publishes the strict Parchment Cream repair, then verify the checkpoint/version, live response, Parchment Cream availability, and continued Explore availability; checkpoint `28fe6e07` saved and auto-published. Initial public probe showed promotion lag; after the subsequent startup, all three public domains served `index-Ctkp4WKe.js` containing `Parchment Cream`, `#F1E5D0`, and `#8F6418`, and `/explore` returned HTTP 200.
- [x] Create a narrow GitHub `main` reconciliation that retains only the strict Parchment Cream repair and restores each held splash/video/proxy path to managed baseline `7777b669`; validated and pushed GitHub `main` commit `54795c2e8b27724610648e92281f95e4c7816178`, whose parent is `3029067e` and whose seven-path delta restores the held preload, desktop crop/veil, CinematicSplash, cinematic contract, proxy route, server mount, and managed version marker to the baseline without changing Explore.
- [x] Retry the authorized managed checkpoint after the safe GitHub mainline reconciliation, automatically publish only the strict theme repair, and verify live Parchment Cream and Explore availability. Checkpoint `28fe6e07` is live; runtime startup completed cleanly, all public domains serve the current theme bundle, and Explore remains available.
- [ ] Conduct a read-only live-surface review of the released Living Nexus public journey—entry, Home, Explore, Work, Creator, Register, and navigation—across desktop and mobile; identify the first evidence-backed friction point and present a bounded repair proposal before any source change or publication.
- [x] Implement the Keeper-approved path-aware cinematic entry boundary: preserve the full Home (`/` and `/home`) ceremony for a fresh session while allowing fresh direct links to `/explore`, `/song/:id`, `/creator/:id`, and `/manifest` to render immediately without setting the existing splash-session flag; changed only `App.tsx` and `server/tests/cinematicEntryBoundary.test.ts`. Focused test, typecheck, and production build passed; desktop/mobile captures confirm the expected Home-versus-deep-link behavior. Awaiting Keeper visual-release approval before checkpoint or publication.
- [x] Trace the selected-genre data path from upload/preparation through Draft, existing WID assignment, publication, Edit Work rehydration, and public Work retrieval; corrected the selected-genre seam so comma-separated selections including `Other` are preserved, without changing WID/provenance semantics.
- [x] Add creator-date fields at the existing upload metadata boundary after classifying them as WID-bound, editorial, or independent manifestation data; added creator-declared Original Creation Date (existing `releaseDate`) and Creator Release Date (`creatorReleaseDate`) as editorial Work metadata. The new additive database column was applied and recorded in `drizzle/migrations/0134_add_creator_release_date.sql`.
- [x] Add regression coverage proving `Other` persists through publication into Edit Work and public Work metadata, plus the approved creator-date field continuity; focused coverage passed 21/21, typecheck and production build passed, and the full suite found only the known unrelated held CinematicSplash CSS-marker assertion failure. Visual release confirmation remains pending.
- [x] Add creator-controlled historical Work date fields to the Music Register and Edit Work flows, persist them as Work metadata, and distinguish them in UI/contracts from immutable system registration/WID and publication timestamps. System timestamps remain excluded from editable inputs.
- [x] Extend the existing metadata-revision provenance path so a creator-initiated Work-date change creates an append-only provenance event without changing the WID, WID assignment timestamp, or publication timestamp. Genuine changes now record `creator_historical_dates_revised` with prior/current values and immutable WID context.
- [x] Complete the `Other` genre continuity repair across the existing upload, metadata update, Edit Work, and public Work surfaces, with regression coverage through publication; selection chips preserve `Other` alongside all selected genres and the public Work view renders each stored genre. The upload UI structure remains intact.
- [x] Create one non-production current-state versus destination visual direction board for Keeper review; preserved the Parchment Cream, cathedral, music/provenance, mobile-priority, and creator-sovereignty constraints, with no source, data, or release change.
- [x] Conduct a read-only open-source architecture assessment of Funkwhale for Living Nexus: inventory current audio/media infrastructure, classify CORE/EXTEND/COMMODITY, evaluate current Funkwhale capabilities, define the sovereignty-preserving system boundary, assess licensing/maintenance/upstream contribution opportunities, and propose only the smallest future proof of concept; no source, schema, data, routes, deployment, or configuration was changed. Assessment saved as `/home/ubuntu/funkwhale_open_source_architecture_assessment.md`.
- [x] Produce the read-only Living Nexus Open-Source Dependency and Contribution Map, preserving Funkwhale as Entry 1 and auditing identity/authentication, media provenance, signing/transparency, search/discovery, object storage, federation, media processing, creator/social infrastructure, observability/administration, backups, and creator economics. For each category, map current implementation to mature OSS candidates, license, maturity, maintenance, security, integration complexity, potentially replaceable code, required Living Nexus core, upstream contribution opportunities, and a KEEP / ADAPT / REPLACE / EXTRACT / UPSTREAM / DEPRECATE recommendation. No production code, schema, data, routing, deployment, or configuration was changed; the governing map is saved at `/home/ubuntu/living_nexus_open_source_dependency_contribution_map.md`.
- [x] Distill the approved Open-Source Dependency and Contribution Map into a single-page Living Nexus Architecture Constitution defining what Living Nexus is, must own, may delegate, must never delegate, and the dependency-admission rules; no product implementation, schema, data, deployment, or configuration change. Constitution saved at `/home/ubuntu/living_nexus_architecture_constitution.md`.
- [x] Upload the Living Nexus Open-Source Dependency and Contribution Map and Architecture Constitution to the user’s Google Drive without changing product implementation. Uploaded as Drive files `1I3SmO-B3djtnPeKJivMKKgwtjGCSgygS` and `11b7LueETjOAVJ6Ra9XusIQu7xONWvzM5`.
- [x] Diagnose the live Other-genre loss from publication through Edit Work and determine whether it is a missing release, storage/retrieval defect, or display regression before proposing any repair. The exact report is legacy `songId: 1` / `WID-MUS-C1FCC012-70E4B7CC` (**Fix DA Server**), which has `genre = NULL`, no dates, and no `workEvents`; a control specimen proves the live database/API/public Work/Edit Work paths preserve `Other`. This is a historical missing creator declaration, not a current global loss; a single owner-authorized amendment remains queued.
- [x] Align the held creator-date repair with the explicit creator-facing names **Creation Date** and **Original Release Date**, while preserving immutable system-generated WID Assignment Date and Publication Date; creator-date revisions remain append-only provenance events and system times are excluded from the editable contract.
- [x] Under Keeper approval, checkpoint and auto-publish the validated creator-date and genre-continuity repair, then verify the released metadata contract before changing any record. Checkpoint `0b8c73e8` published successfully; public `index-zJoNlSq8.js` and `ManifestationStudio-CPbbT-wK.js` expose the Creation Date, Original Release Date, and creatorReleaseDate paths, while direct `/manifest`, `/explore`, `/song/1`, `/creator/1`, and Verify routes returned HTTP 200. A My Browser Register inspection also confirmed the direct route reaches the Music Register without the cinematic gate.
- [x] Apply the single owner-authorized amendment for `songId: 1` / `WID-MUS-C1FCC012-70E4B7CC` (**Fix DA Server**) by setting only `genre = Other`; appended `workEvents.id = 1` / `creator_genre_amended` with prior null/current Other, Keeper actor attribution, immutable WID context, and explicit unchanged WID-assignment/publication-time flags. Verified public Work rendering and owner Edit Work rehydration; WID, Published status, public visibility, and creation time remain unchanged.
- [x] Create a non-production visual proposal of the approved Creation Date/Original Release Date calendar controls, their validation and immutable-system-time explanation, and the public read-only Work provenance-history section before any implementation or publication.
- [x] Create a non-production current-product flow map that places creator date selection, immutable system chronology, and public provenance history across Register, preparation, WID assignment, publication, Edit Work, public Work, and Verify. Saved at `/home/ubuntu/living-nexus-date-provenance-current-product-flow.png` with deterministic Mermaid source.
- [x] Draft, without applying, the code changes for accessible Creation Date and Original Release Date controls in Register/Edit Work and the public Work provenance-history section, including the migration and append-only Work-event contract. Saved at `/home/ubuntu/living-nexus_creator_dates_provenance_implementation_draft.md`.
- [x] Implement the approved accessible Creation Date and Original Release Date calendar controls with shared chronology validation across Register and Edit Work, preserving the existing editorial/WID boundary. Added shared `HistoricalDateField` calendar controls and `workHistoricalDates` validation utilities.
- [x] Record initial creator-date declarations at upload and expose a public read-only Work provenance-history timeline for creator date declaration and revision events. Reused append-only `workEvents`, added public `songs.creatorDateHistory`, and mounted `WorkProvenanceHistory` on public Work pages.
- [x] Validate the completed creator-date/provenance implementation in the currently attached My Browser session before requesting release approval. In My Browser, public `/song/1` visibly rendered the protected read-only Provenance History and owner Edit Work rendered both date triggers plus the system-record notice; the Creation Date trigger opened an interactive, navigable calendar. No metadata was saved during validation.
- [x] Correct the shared calendar-popover portal boundary: My Browser shows both date triggers in the Creative Drawer but the calendar is not visible because the project Popover wrapper ignores the requested dialog container. Preserved the date contract and fixed portal containment by forwarding `container` to `PopoverPrimitive.Portal`; My Browser confirms the corrected picker now opens above the Edit Work overlay.
- [ ] Conduct a read-first discovery of the existing Creative Chapel/Cathedral and creator workflow surfaces, then propose an expandable, mobile-safe Creative Cathedral that makes work preparation, testimony, creator-declared chronology, and provenance posture easier to understand without changing WID, publication, or existing creator flows until separately approved.
- [x] Draft the Creative Cathedral agentic-assistant component architecture and proposed persistence model for creator workspace state and provisional AI suggestions adjacent to Work provenance. Defined explicit creator consent gates, private/mutable versus append-only boundaries, source/proposal/approval state, and public disclosure posture; no production code, schema, data, routes, deployment, or configuration was changed. Draft saved at `/home/ubuntu/living_nexus_creative_cathedral_agentic_architecture_draft.md`.
- [x] Convert the Keeper-accepted Creative Cathedral Slice 1 constitution into a bounded ADR and implementation plan only: private creator workspace persistence, creator-approved context inspection, structured non-binding suggestions, explicit Apply-to-form actions, and unchanged Register/WID/publication/provenance authority. Included exact components, procedures, schema migration, authorization, privacy, validation, rollback, and release boundaries; no production code was implemented. ADR saved at `/home/ubuntu/living_nexus_creative_cathedral_slice1_adr.md`.
- [x] Implement the additive creator-private Creative Cathedral session, suggestion, and decision schema plus migration without altering songs, WIDs, publication, or workEvents. Migration `0135_add_creative_cathedral_workspace.sql` was applied and the three private tables/indexes were verified.
- [x] Implement creator-only Cathedral tRPC procedures for session continuity, explicit context consent, one structured media-facts suggestion, Apply-to-form, Edit-first, Dismiss, and private workspace cleanup; authoritative Work and provenance writers remain unreachable from the Cathedral router.
- [x] Extract structured metadata suggestion logic into a shared internal service and add optional bounded model selection to the existing LLM helper without changing existing callers' default model behavior.
- [x] Implement the expandable Creative Cathedral workspace in Music Register with desktop contained-panel and mobile full-height-sheet behavior, explicit context disclosure, suggestion evidence, local-form-only application, and safe rejection of oversized legacy context.
- [x] Add authorization, allowlist, consent, WID/upload invariance, migration, responsive, context-normalization, and no-authoritative-write regression tests for Creative Cathedral Slice 1.
- [x] Validate Creative Cathedral Slice 1 with typecheck, focused tests, full Vitest, production build, refinement, desktop/mobile evidence, and an authenticated My Browser smoke. The bounded model request returned a private non-binding suggestion; Dismiss recorded correctly; the exact test workspace was removed. Full suite remains 535 passed, 1 skipped, and one known pre-existing held CinematicSplash CSS-marker failure. No checkpoint or publication was created.
- [x] Diagnose and repair the creator-reported Creative Cathedral failure for `SUPERCHARGE ME.mp3`: the original structured-output failure was followed by a real client parser defect (`Buffer is not defined` / externalized Node buffer). Added the bundled browser Buffer dependency before metadata parsing, normalized model output conservatively, and preserved the no-Work-change failure behavior.
- [x] Expand the creator-approved Cathedral evidence manifest to visibly enumerate extracted audio tags, technical audio facts, embedded artwork presence, lyrics/notes, production hints, attached visual state, and existing form values before any model call. My Browser visibly showed filename, title, artist, album, genre, BPM, key, embedded date, codec, duration, sample rate, channels, bitrate, production hint, and current-form scope for the tagged fixture.
- [x] Add selective AI-assisted Details proposals for title, genres, BPM, key, moods, caption, and creator-declared dates only when supported by approved evidence; confidence and evidence remain visible, every proposal initializes unselected, and explicit field selection plus Apply selected is required to alter local form state.
- [x] Add evidence-bound Participation proposals for music, lyrics, and voice only when creator-provided comments or production metadata specifically supports that axis; never invent contributor identities, ownership, percentages, testimony, or signatures. Normalization removes unsupported axes and rebuilds the summary from the surviving patch.
- [x] Add regression coverage for browser metadata parser setup, approved evidence schema, explicit consent, conservative normalization, axis-specific Participation gating, date precision, selective local apply, zero default selection, no-authoritative-write invariants, and bounded failure behavior. Focused Cathedral suite passed 15/15; combined Cathedral/prepared-registration/metadata suite passed 27/27 after final test additions.
- [x] Validate the expanded Creative Cathedral flow with tagged `SUPERCHARGE ME.mp3` context in My Browser without registering, publishing, issuing a WID, or altering provenance. Exactly one consented review returned in 14.4s, field evidence/confidence rendered, only Genres was explicitly selected and locally applied, and request logs showed no Work-authority endpoint. The exact private validation session/suggestion/decision rows were removed afterward. Checkpoint and publication remain held for Keeper review.
- [ ] Define the Creative Cathedral as a creator-controlled agentic registration workspace: inspect uploaded media, explain extracted metadata, propose non-binding genre/date/credit/testimony drafts, flag missing provenance context, and require explicit creator approval before any write, registration, publication, or provenance event. Present the flow and bounded implementation slice before code changes.
- [x] Reconcile each normalized Cathedral proposal summary with the evidence-gated patch so removed unsupported participation/date claims are not still described as proposed fields. Creator-facing summaries are now deterministic from the final conservative patch.
- [x] Preserve date precision in audio evidence: accept only a full embedded `YYYY-MM-DD` Original Release Date and omit year-only tags rather than inventing January 1.
- [x] Complete final validation for the held creator-date plus evidence-assisted Cathedral bundle: `pnpm check` passed; focused combined suite passed 27/27; production build passed; `git diff --check` passed; full suite reached 541 passed, 1 skipped, with only the known pre-existing CinematicSplash `49.8%`/`50.2%` marker failure; refinement retained the existing 69/100 baseline with API consistency and performance passes. Desktop evidence flow and 390×844 Register rendering were reviewed. No checkpoint or publication was created.
- [x] Expand Creative Cathedral typography, contrast, and creator-controlled workspace size so evidence and suggestions do not disappear into the background and desktop/mobile users can open a materially larger working surface. Desktop now has a high-contrast inline steward panel plus an explicit 760px expanded workspace; mobile retains a near-full-width overflow-safe Sheet.
- [x] Constrain selected audio filenames and preview titles within their upload/preview boundaries across desktop and mobile, including long uninterrupted names. Long-filename My Browser validation remained inside the upload block and title values retain full accessible `title` text.
- [x] Make embedded, uploaded, and generated artwork sources visually distinct and provide an unmistakable Replace artwork control with an accessible source label. The preparation tile and preview now label Unbound, Embedded, Creator upload, Generated, or Remixed state without changing the registration payload.
- [x] Reuse the existing song `collectionId` album linkage and creator-domain album data to surface a more visible album showcase on creator domains without changing schema, WIDs, or provenance. Albums remain routed through `/creator/:handle/albums`; visitor collection projections now exclude private/unlisted collections while owners retain them.
- [x] Add focused UI/contract regressions and validate the refined Cathedral, upload, artwork, and album surfaces on desktop and mobile. Focused combined suite passed 35/35; final full suite reached 549 passed and 1 skipped with only the known pre-existing CinematicSplash `49.8%`/`50.2%` marker failure; typecheck, production build, diff hygiene, and the 69/100 refinement baseline completed.
- [x] After all validation passes, checkpoint and automatically publish the combined held creator-date, provenance-history, Creative Cathedral, and current UI refinement release authorized by Doc.
- [x] Repair the desktop Music Studio split-layout height and scroll containment discovered during validation so the preview rail can reach Creative Cathedral instead of clipping content beneath the viewport. Desktop panels are viewport-bounded with independent contained scrolling; mobile retains normal document flow.
- [x] Map the existing album/collection relationship, owner visibility, Register payload, Edit Work metadata path, and WID/provenance behavior before adding a creator-facing assignment control. The slice reuses only legacy creator-owned `collections` / `songs.collectionId` / `WID-ALB` records—not personal playlists or manifested `WID-COL` collections.
- [x] Add an explicit creator-controlled album assignment selector to Music Register using only the existing owner-scoped `collectionStudio.listMine` reader and the existing `collectionId` relationship. The unassigned option is explicit; selection remains editorial and outside WID serialization.
- [x] Add an explicit creator-controlled album reassignment/removal selector to both established Edit Work surfaces using the existing metadata authority. `songs.updateMetadata` owner-validates the selected collection and reuses existing collection link/unlink helpers; Work WID, Work provenance, publication, and album identity remain untouched.
- [x] Add focused regression coverage for owner-only collection options, Register payload assignment, Edit reassignment/removal, no collection creation, no WID reissue, and no provenance mutation. Typecheck passed; focused suite passed 32/32; full suite reached 553 passed and 1 skipped, with only the known unrelated CinematicSplash marker assertion failing; production build and diff hygiene passed; refinement remained at the pre-existing 69/100 baseline.
- [x] Run authenticated live smoke tests on the published domain for Creative Cathedral typography/entry and the creator-domain album shelf. `/manifest` showed readable published Cathedral entry and visual-source labels without media attachment or mutation. `/creator/DocSMercer/albums` loaded five linked WID-ALB albums with track counts and WIDs. No Work, WID, publication, storage, or provenance mutation was exercised.
- [x] Confirm the canonical chooser relationship is already label-associated inside the actual Music Register workspace; the failed automation attempt targeted its separate landing-page entry button, not the real `music-register-audio-file` input. No additional input change was required for this slice.
- [x] After validation, checkpoint and automatically publish the album-assignment release authorized by Doc, then verify public bundle and route delivery.
- [x] Map the creator-page Arrange domain trigger, editor surface, owner authorization, persistence procedure, stored layout model, and creator-page rehydration path before changing code. `LoopCreatorPage` opens `DomainEditor` and renders `DomainRenderer` for the same creator ID; both editor and renderer use `domain.getLayout`, while protected `domain.saveLayout` persists creator-scoped blocks.
- [x] Repair the evidenced Arrange domain rehydration seam so creator-owned arrangement changes reflect accurately in the owner and public creator page. Editor hydration now initializes only when the persisted layout signature changes, avoiding the repeat state reset that made opening Arrange domain time out; the query is also gated until a valid creator ID exists.
- [x] Add focused regression coverage for owner authorization, arrangement save/read reflection, query invalidation/hydration, stored visibility and position ordering, and unchanged WID/provenance/publication boundaries. The new Arrange domain contract passed 3/3; typecheck passed; full suite reached 556 passed and 1 skipped with only the known unrelated CinematicSplash marker assertion failing; production build and diff hygiene passed; refinement held the pre-existing 69/100 baseline.
- [x] Run a bounded authenticated creator-domain smoke using one reversible arrangement change, verify it persists after refresh in the intended owner/public projection, then restore the initial arrangement. Provenance Trail moved hidden → visible (Domain v10) → hidden (Domain v11) and reflected immediately plus after two fresh loads; only `domain.saveLayout` / `domain.getLayout` traffic appeared.
- [x] Save and publish the verified Arrange domain repair under Doc’s standing authorization; then verify the public bundle and creator route delivery.
- [x] Audit the highest-friction creator and public paths after the Arrange release, record the next smallest evidence-led improvement, and publish it only after tests and responsive validation pass. The confirmed next gap was public Work → existing parent album continuity.
- [x] Add a read-only public Work → parent album continuity link using the existing `songs.collectionId`, `songs.getCollectionForSong`, collection visibility, creator route, and WID-ALB model; do not create or mutate albums, WIDs, provenance, or publication state. The query now returns a collection only when the established `visibility` enum is `public`, and the Work provenance column shows **Part of album** with the parent WID-ALB link.
- [x] Add focused coverage for assigned public album display, absent/unassigned suppression, private/unlisted album suppression, and unchanged Work authority boundaries. Focused continuity, assignment, and album-showcase coverage passed 9/9; the production guard uses `collection.visibility === "public"`, not a nonexistent `isPublic` field.
- [x] Validate public Work → album continuity at desktop and mobile, then checkpoint and publish the bounded read-only discovery improvement under Doc’s standing authorization. Public specimen `Are You Insane (1)` displayed `Extraction & Testimony` / `WID-ALB-76B2700A-E43F11BE`, and the established `/creator/DocSMercer/albums` destination loaded five albums. Desktop/mobile visual checks passed; full suite reached 559 passed and 1 skipped with only the known unrelated CinematicSplash marker assertion failing; typecheck, production build, diff hygiene, and the 69/100 refinement baseline completed.
- [x] Map the expanded global-player overlay geometry, lower-content scroll behavior, z-index/portal interaction, and all visible player/action route destinations at 100% desktop zoom before changing code. The scan confirmed stale desktop bottom-player allowances, TopBar-local shuffle/repeat state, misleading View Queue routing, and a creator-wide rather than specific parent-album link.
- [x] Repair the evidenced expanded player viewport/scroll containment issue so every existing lower control, Work summary, and provenance section remains reachable at normal desktop zoom without altering playback or queue semantics. Desktop now removes the obsolete 130px page pad and gives the centered modal `min(780px, calc(100dvh - 96px))` with flex-safe minimum height.
- [x] Mechanically verify visible global-player actions and the adjacent route buttons, repair only confirmed broken/misleading destinations or inaccessible controls, and preserve existing authentication, payment, WID, provenance, and publication authority. At 100% desktop zoom, View Queue kept the current Work route and focused the embedded queue; TopBar shuffle changed the shared player hint off → on → off; the direct public parent-album route loaded a 12-track existing WID-ALB album. No Work, queue persistence, WID, provenance, publication, storage, or payment mutation occurred.
- [x] Add focused player-layout and route-action regression coverage; validate the repaired expanded player at desktop/mobile and 100% desktop zoom, then checkpoint and publish the bounded repair under Doc’s standing authorization. Focused player/album contracts passed 6/6; typecheck and production build passed; full regression reached 562 passed and 1 skipped with only the known unrelated CinematicSplash marker assertion failing; refinement retained the 69/100 baseline; mobile and desktop Work/album visual checks were captured.
- [x] Remove stale desktop bottom-space left by the former bottom-player layout so the expanded player’s lower contents remain reachable at 100% zoom without changing player state, portal layering, or scroll semantics. The adjacent FloatingAvatar offset remains intentionally unchanged because it did not overlap the validated expanded player.
- [x] Route TopBar shuffle/repeat through PlayerContext, change expanded-player View Queue to its existing expanded player queue, and route public Work parent-album links to the existing specific `/album/:collectionWid` surface. View Queue now scrolls to the real in-player queue instead of merely expanding an already expanded window.
- [x] Add focused regression coverage for global player geometry/action wiring and direct album routing; validate all repaired actions at desktop/mobile before checkpointing and publishing.
- [x] Save the validated global-player access and route-action repair as a checkpoint under Doc’s standing release direction, then verify promotion before claiming a live update.
- [x] Map the Home limited showcase data shape, existing creator identity fields, card/link structure, and desktop/mobile layout before adding creator orbs. The public FeedRow already supplies `creator.profilePhotoUrl`, `artistHandle`, `name`, and `id`; Home had no visible orb on showcase cards.
- [x] Add an accessible creator profile orb and an explicit clickable creator name/handle to each Home limited showcase Work card, linking to the existing creator domain when identity is available. The Work-card creator row and featured creator pills now show compact public profile orbs; creator text remains the readable link name.
- [x] Preserve Work-card, Support, discovery ranking, WID/provenance, and fallback behavior; do not fabricate profiles, avatars, or creator routes for incomplete records. Missing public imagery falls back to an initial/avatar icon; missing handle falls back only to an existing numeric creator route or Explore.
- [x] Add focused Home showcase identity-link coverage and validate desktop/mobile Home presentation plus direct creator-domain navigation. Focused Home/creator coverage passed 7/7; typecheck and production build passed; full suite reached 563 passed and 1 skipped with the known unrelated CinematicSplash marker assertion; Home DOM evidence confirmed six profile-orb card rows and 15 existing creator anchors after data loaded.
- [x] Save the validated Home showcase creator-identity improvement as a checkpoint, then verify public promotion before claiming a live update.
- [x] Map existing Home creator identity inputs, public avatar/sigil data, creator activity signals, and visual tokens before enlarging the creator layer. Existing `profile.featuredCreators` supplies public profile photo/banner, name/handle, ID, and published Work count; Home already has canonical creator routing.
- [x] Add larger Home creator identity cards—approximately half the showcase Work-card scale—with a circular public avatar/sigil above a readable handle plate and direct creator-domain link. The dedicated **Creator identities** layer renders a responsive 2/3/4-card grid beneath Limited Showcase.
- [x] Ensure newly authenticated creator profiles become eligible through the existing live public creator projection and bounded query refresh once they have a real public identity plus at least one Published Work; avoid a background worker unless current event/data flow proves one is necessary. Home refreshes the public query after 60 seconds and on focus only while the page is open.
- [x] Tighten the existing public creator projection so Home eligibility requires at least one Work with both `status = Published` and `isPublic = true`, preventing a legacy Published-but-private row from exposing a creator card.
- [x] Derive creator-card accent color/glow/texture only from existing public profile uploads and verifiable activity/utilization fields; do not fabricate identity, engagement, or creator imagery. Avatar/banner inputs are public creator uploads, ID selects a stable visual hue, and published Work count controls only visible copy and aura intensity.
- [x] Preserve showcase Work playback, Work card, Support, discovery ordering, WID/provenance, and creator authority boundaries while adding the identity layer. This change is a read-only public projection.
- [x] Add focused contracts and validate desktop/mobile scale, link routing, accessible names, and public fallback behavior before checkpointing and publishing the Home identity refinement. Typecheck and focused coverage passed 6/6; full suite reached 565 passed and 1 skipped with only the known unrelated CinematicSplash marker assertion; production build and diff hygiene passed; desktop authenticated Home rendered six cards and direct `@DocSMercer` navigation reached the existing creator domain. The screenshot runner’s mobile Home snapshot remained blank at the cinematic entry boundary, while the mobile 2-column containment is enforced directly by the rendered responsive grid classes.
- [x] Save the validated creator-ready Home identity refinement as a checkpoint, then verify public promotion before claiming a live update.
- [x] Inventory all available Living Nexus color schemes and their current Home identity token mappings: Cathedral Dark, Crimson, Illuminated Gold, and Parchment Cream.
- [x] Verify the latest Home creator identity layer under every available color scheme at desktop and mobile sizes. All four desktop schemes rendered the six-card public creator layer without contrast, route, or horizontal-containment failures; the 390px Home capture remained at the intentional cinematic entry boundary, while source confirms the color-independent two-column mobile grid and `min-w-0` containment.
- [x] Check Home creator-card contrast, handle legibility, avatar/sigil treatment, layout containment, and direct creator-domain routes; report only evidenced issues before making any repair. Existing canonical @DocSMercer navigation remained valid. The only observed candidate is deliberate ellipsis for one long handle; it does not overflow but could be made more discoverable later with a visible secondary name or expanded-on-focus treatment.
- [x] Verify Creative Cathedral color-scheme parity across its inline panel and expanded desktop/mobile workspace: surface separation, evidence and proposal legibility, consent controls, authority copy, and overflow behavior. The intentionally dark Cathedral work surface remains legible under all four schemes; the mobile Register capture confirmed contained navigation and preparation UI. No media, suggestion, Work, WID, publication, or provenance action was invoked.
- [x] Map Home handle-plate disclosure, Creative Cathedral accent tokens, all public creator-domain link surfaces, and Arrange-domain entry points before implementation.
- [x] Implement accessible full-handle disclosure on Home creator identity cards without weakening compact mobile containment or direct domain routing.
- [x] Fine-tune Creative Cathedral accent treatments using existing theme tokens while retaining its protected dark evidence workspace and authority semantics. Corrected late-theme accent reassertion and per-scheme action-label colors after contrast calculation exposed insufficient Crimson/Parchment action contrast.
- [x] Run actual mobile contrast checks for the Home identity layer and Creative Cathedral in available schemes, recording any limitations honestly. The mobile capture reached the protected Home cinematic entry and Register update modal, so lower-surface visual inspection remained limited; source gutters plus deterministic four-scheme contrast checks passed.
- [x] Mechanically verify public creator-domain links and owner Arrange-domain entry/save routes across surfaced site locations; repair only confirmed defects. Fixed the `/@handle` creator-domain route, which had returned 404 because its string pattern was unsupported by the active router. Replaced PlayerBar and GlobalPlayer display-name fallbacks with canonical handle/ID routing. Canonical `/creator/DocSMercer`, repaired `/@DocSMercer`, and owner Arrange-domain entry all resolved without a data mutation.
- [x] Add focused contracts, validate desktop/mobile behavior and route integrity, then checkpoint and publish the verified creator experience refinement. Focused Home/Cathedral/creator-route/Arrange-domain coverage passed 19/19; deterministic Cathedral text/action contrast passed across all four themes; typecheck and production build passed; full suite reached 570 passed and 1 skipped with only the known pre-existing CinematicSplash marker assertion failing; diff hygiene passed.
- [x] Trace the Explore Browse Creators control, route/query state, and rendered creator-discovery result to determine why it appears tracks-only. The control entered `viewMode = grid`, but that branch grouped the current `songs.exploreIndex` track rows by creator and left supplemental track strips above it.
- [x] Record the evidenced creator-discovery diagnosis and a minimal Explore repair boundary before source changes.
- [x] Repair only a confirmed Browse Creators routing, data-projection, or presentation defect; preserve Explore’s music-only scope, public visibility, Work authority, WID, provenance, and publication boundaries. Browse creators now uses existing `profile.allCreators`, shows direct creator-domain cards, hides track strips in the dedicated directory mode, and accepts `?view=creators` as a stable shareable state. Shared public creator eligibility now requires both Published and `isPublic = true` Works.
- [x] Isolate the observed local Explore maximum-update-depth/skeleton incident before further creator-directory changes; document the exact trigger and repair only its root cause. The browser console record predates the final Explore HMR change and could not be reproduced in the isolated renderer. The confirmed current defect was presentation: tracks appeared above the creator directory. No speculative render-loop source change was made.
- [x] Validate creator discovery at desktop/mobile and add focused route/data contracts before checkpointing and publishing the verified improvement. Creator-directory contracts passed 3/3; desktop and 390px mobile captures showed Browse creators as first content with 11 public creator cards and no track rows above them; a card destination route was checked without data mutation. Typecheck and production build passed; full suite reached 573 passed and 1 skipped with only the known pre-existing CinematicSplash marker assertion failing; diff hygiene passed.
- [x] Save the verified Browse creators public-directory repair as a checkpoint and hold its separate public promotion for the project Publish action.
- [x] Reproduce and isolate the authenticated `/explore?view=list` maximum-update-depth error from the supplied runtime trace, including the external-store update path. The supplied and local traces identified TanStack Query observer churn on list-only mounted rows; no creator-directory, Work, WID, or provenance state was implicated.
- [x] Run one temporary, URL-gated static-row diagnostic to determine whether list-row hook consumers are required for the authenticated render loop; remove the probe after diagnosis. The diagnostic was removed; source and request evidence confirmed the list was the only surface mounting individual status observers.
- [x] Record the evidenced root cause and restrict the repair to the confirmed Explore list-mode state seam.
- [x] Add focused regression coverage for authenticated-style Explore list mode and verify creator-directory mode remains stable. The contract requires list rows to receive an explicit bulk-map boolean, stable Explore-derived data, and no retained diagnostic path.
- [x] Run typecheck, full regression, production build, and route smokes before checkpointing the verified Explore stability repair. Typecheck, focused Explore contract 4/4, production build, and diff hygiene passed. Full regression reached 574 passed and 1 skipped, retaining only the known pre-existing CinematicSplash marker assertion. No further `Maximum update depth` event or individual `songs.getLikeStatus` request appeared after the 05:03:34 repair HMR boundary.
- [x] Save the verified Explore list-mode stability repair as a checkpoint and hold public promotion for the project Publish action.
- [x] Capture the reported Work hero misalignment at normal 100% desktop and mobile geometry, separating browser zoom effects from actual page composition. The 25% browser zoom exposed, but did not cause, the underlying `88vh`/`justify-end` hero geometry that pushed the record anchor to the viewport floor.
- [x] Record the evidenced Work hero root cause and a bounded alignment/viewport repair before source changes.
- [x] Repair only the confirmed hero positioning, scale, or scroll-containment defect; preserve Work data, WID/provenance, player, and creator-action authority. The hero now uses bounded `clamp(34rem,68svh,46rem)` height, vertically centered content, centered actions, and a closer first-record seam; heading hierarchy was strengthened without any record mutation.
- [x] Add focused composition contracts and validate desktop/mobile Work access before checkpointing the verified repair. Hero contract passed 3/3 with parent-album continuity 3/3; typecheck/build/diff hygiene passed. Full regression reached 577 passed and 1 skipped, retaining only the known pre-existing CinematicSplash marker assertion. Full-page desktop and 390px mobile captures show title, actions, and the first record panel centered, legible, and contained.
- [x] Save the verified Work-page composition repair as a checkpoint and hold public promotion for the project Publish action.
- [x] Map the inline TopBar player, search field, and header breakpoints causing the 150%-zoom collision.
- [x] Remove only the TopBar search field that collides with the inline player; preserve the inline header player, PlayerContext, global player, queue, expanded-player, and playback controls. The TopBar autocomplete observer and rendered form are removed; `InlinePlayer` is retained and widened from 700px to 920px maximum width.
- [x] Validate the cleared TopBar search zone and retained inline/global player access at ordinary desktop, zoom-constrained desktop, and mobile layouts before checkpointing the refinement. TopBar contract passed 3/3; inline player remains mounted and mobile header behavior remains unchanged because TopBar is desktop-only.
- [x] Map the 150%-zoom Explore composition target and its normal-scale responsive constraints before implementation.
- [x] Remove the TopBar search field and retain the inline TopBar player plus existing global-player surfaces and controls.
- [x] Refine normal-100% Explore spacing and card geometry to reproduce the clearer 150%-zoom hierarchy without relying on browser zoom. The desktop reading plane is bounded at 1360px; Explore heading scales to 3.25rem maximum; desktop showcase cards expand to 12rem with larger title and creator labels. Explore’s own local search and creator filter remain intact.
- [x] Validate the normal-scale Explore target, retained player access, and mobile containment before checkpointing the refinement. Typecheck and focused TopBar/Explore contracts passed 5/5; production build and diff hygiene passed. Full regression reached 579 passed and 1 skipped, retaining only the known pre-existing CinematicSplash marker assertion. Browser automation was blocked by the existing update notice, so the final live visual assertion is limited to the isolated 1280px renderer and source-level responsive contracts.
- [x] Save the verified normal-scale Explore and TopBar search-removal refinement as a checkpoint and hold public promotion for the project Publish action.
- [x] Confirm the current rendered TopBar search removal and Explore view-control state in browser and source. The authenticated local browser shows no top-navigation search field; the remaining `Search works, creators, genres…` input is the intentional page-local Explore filter. It exposes exactly List and Browse creators controls.
- [x] Remove the legacy Explore Columns toggle, URL/default state, and rendered column presentation while preserving List and Browse Creators. The toggle now exposes only List and Browse creators. `view=grid` and `view=columns` resolve to the retained List state; the Columns icon, state branch, and renderer are removed.
- [x] Add focused control-state coverage and verify local browser plus responsive source behavior before checkpointing the refinement. Explore/TopBar contracts passed 10/10; `view=grid` rendered only List and Browse creators in the authenticated local browser, with the intended page-local filter and no TopBar search. Typecheck/build/diff hygiene passed; full regression reached 580 passed and 1 skipped with only the known pre-existing CinematicSplash marker assertion.
- [ ] After publication, verify the public web page exposes no TopBar search and only List plus Browse creators on Explore.
- [x] Map active public creator-page data, canonical creator URLs, existing QR facilities, and reusable identity imagery before adding a Creator Witness Card.
- [x] Reuse and refine existing QR/card components and routes where available; do not introduce a competing creator-identity artifact system. Reused `QRIdentityCard`, `QRShareModal`, and the established 480×760 downloadable canvas artifact.
- [x] Define a print-safe Creator Witness Card using only public creator-owned banner, avatar, sigil, and declared identity assets; never generate or fabricate faces/likenesses.
- [x] Add a creator-page card with a scannable QR code resolving to the canonical creator domain and a concise source-labelled identity/testimony explanation. Every Loop creator hero now exposes **Creator card**, which opens the refined existing artifact and encodes `${origin}/creator/${handle-or-id}` directly.
- [x] Add robust image/sigil fallback and validate QR payloads, desktop/mobile print composition, accessibility, and all public creator routes before checkpointing. Creator-card and route contracts passed 8/8; typecheck, build, and diff hygiene passed. Public creator desktop/mobile captures show the control in the existing action hierarchy. Full regression reached 584 passed and 1 skipped, retaining only the known pre-existing CinematicSplash marker assertion.
- [x] Save the verified Creator Witness Card refinement as a checkpoint and hold public promotion for the project Publish action.
- [x] Measure the current Creator Witness Card source-image crop, text/QR balance, and mobile modal framing before changing composition.
- [x] Define a centered, full-bleed creator-owned art treatment that preserves QR quiet zone, creator identity, declared testimony, and print-safe frame.
- [x] Preserve the complete creator-declared testimony in a protected readable plane and retain full-text access; do not truncate, obscure, replace, or subordinate it to image motion.
- [x] Refine existing card canvas cropping and modal preview geometry so visual art fills its block with a deliberate centered focal point rather than passive banner scaling.
- [x] Validate scannable QR contrast, identity/testimony readability, print composition, and mobile card containment before checkpointing the visual refinement. The open local modal rendered full vertical creator art with protected planes; full creator-declared testimony was selectable and reachable after desktop scroll and visible below the card on 390px mobile; the real Download Card control produced a static 480×760 PNG; and an independent decoder read its QR as the canonical `/creator/DocSMercer` route. No document-body overflow was observed.
- [x] Add a restrained, reduced-motion-safe fade-in and subtle backdrop blur to the Creator Witness Card modal without altering testimony, QR, art export, or canonical-route behavior. Local desktop and 390px mobile checks observed both modal animations with a static 8px blur; emulated reduced-motion disabled both animations while retaining the readable backdrop; complete testimony remained reachable and no document-body overflow occurred. Focused contract coverage passed 8/8, typecheck and production build passed, and full regression reached 589 passed / 1 skipped with only the established unrelated CinematicSplash 49.8%/50.2% marker failure.
- [x] Prevent creator-provided character/showcase art on the Creator Witness Card from being mistaken for a creator portrait; anchor identity in verified creator name/handle and source-label the art. The card now labels public visual assets as `CREATOR-PROVIDED ART`; name and handle remain the identity plane, and the card makes no likeness assertion.
- [x] Replace the cramped canvas testimony with a concise source-labelled excerpt or clearly marked AI-assisted Origin summary, while retaining the complete original creator declaration below the card unchanged and selectable. The active safe default is a single-line exact `CREATOR-DECLARED ORIGIN · EXCERPT`; no AI rewrite is generated or stored. The full original declaration remains selectable beneath the card. Focused contract coverage passed 8/8, typecheck and production build passed, and the actual downloaded card QR still independently decodes to its canonical creator route.
- [x] Initialize each Work genre selector from that Work’s persisted genre state only; never append creator-profile genre values to the selection. Music Register now initializes only from its explicit Work prefill, and both existing edit surfaces already initialize directly from `song.genre`.
- [x] Present creator-profile genres as explicitly non-binding suggestions that require a creator’s selection before becoming Work classification metadata. A pure `getSuggestedWorkGenres(profileGenres, workGenres)` now removes existing Work tags and renders optional `+` suggestions only; it does not write or preselect metadata.
- [x] Isolate creator Origin/testimony from classification and participation/authorship displays so no provenance declaration appears in flattened Work metadata. Public Loop Work Origin now reads only `haaiOriginStory`; description and caption remain editorial and a Work without a declaration truthfully renders the no-Origin state.
- [x] Add regression coverage for clean Work genre initialization, suggested—not-inherited profile genres, and Origin isolation across upload/edit/public Work surfaces. Focused contracts passed 28/28; the public Work was rendered with both a declared-Origin specimen and a caption-only/no-Origin specimen at desktop/mobile; typecheck/build passed. Full regression reached 591 passed / 1 skipped with only the known unrelated CinematicSplash CSS-marker failure.
- [x] Add a local Clear All control to every existing Work genre selector so creators can reset only the in-progress Work selection without affecting profile suggestions, Origin, WID, or saved records. Register, Creative Drawer, and Edit Chapel now expose an accessible local `Clear All` action whenever Work genres are selected; it calls only local `setGenre("")` and leaves persistence to the existing explicit save/publish action. Focused cross-surface contracts passed 15/15; TypeScript and production build passed; full regression reached 592 passed / 1 skipped with only the established unrelated CinematicSplash 49.8%/50.2% marker failure. An unauthenticated local browser could not open protected Register details, so UI interaction proof is contract-level; no Work mutation was performed.
- [x] Add a confirmation prompt before locally clearing Work genre selections, while preserving cancellation and existing explicit save/publish authority. Register, Creative Drawer, and Edit Chapel now require an explicit browser confirmation; cancellation returns without changing local selection, and confirmed clearing remains local until the existing Save/Publish action.
- [x] Add a Select All Suggested action that applies only the currently displayed, non-binding profile-genre suggestions to the in-progress Work selection. Music Register uses pure `applySuggestedWorkGenres` to retain already selected Work tags and add only currently unselected profile suggestions after the creator clicks the explicit control.
- [x] Add subtle, reduced-motion-safe selected-genre fade-out feedback before a confirmed local clear completes. Confirmed clearing adds the `work-genre-selection-clearing` class for a 180ms opacity/transform-only exit, while `prefers-reduced-motion: reduce` disables that animation. Focused contracts passed 16/16; TypeScript/build passed; full regression reached 593 passed / 1 skipped with only the established unrelated CinematicSplash 49.8%/50.2% marker failure.
- [x] Diagnose the reported playback interruption and apparent application reset without speculative player, album, route, WID, provenance, or storage changes. The global player is already app-wide with a module-level audio singleton, so ordinary album/page updates do not own its lifecycle. The credible reset vectors are full document reload/redirect paths; the primary confirmed source path was automatic service-worker activation followed by the controller-change reload.
- [x] Instrument development-only player ownership, media-element events, route transitions, app errors, and teardown signals behind an explicit diagnostic flag. Set `localStorage.LN_DIAG_PLAYBACK = "1"` in a development session and inspect `window.__LN_PLAYBACK_LOG`; no diagnostic is active in production or sent off-device.
- [x] Reproduce or isolate the playback-reset trigger, identify the failure domain, and apply only the evidence-backed continuity repair after confirmation. A public Work Listen smoke confirmed normal stream startup with a single navigation and no unexpected reload. Source evidence confirms a service-worker update could cause a document lifecycle teardown, which intentionally pauses and clears the singleton source. Mobile pull-to-refresh, auth expiry, and chunk recovery remain separately recorded full-reload vectors, not conflated with album/query updates.
- [x] Prevent service-worker updates from taking control and reloading an actively listening tab until the listener explicitly chooses the existing update action. Removed installation-time `skipWaiting()`; a worker now waits until the existing update control sends `SKIP_WAITING`. The controller-change reload continues only after this listener-approved update. Focused contracts passed 6/6, TypeScript/build passed, and full regression reached 597 passed / 1 skipped with only the established unrelated CinematicSplash marker failure.
- [x] Diagnose the confirmed desktop Chrome case where an already-playing Work cuts out after the listener switches to Suno, Discord, or another application while the Living Nexus page remains unchanged. The page-state report rules out a document reset for this variant. A controlled Chrome tab background probe normally continued playback, so the intermittent condition remains browser/media-lifecycle dependent; the player now records the exact pause, stall, error, visibility, and recovery chain under an explicit development-only flag.
- [x] Preserve a deliberate listener-initiated pause while detecting and recovering from background media stalls, errors, or browser-initiated pauses without requiring a page refresh. When an actively playing hidden tab emits a pause/stall/error, returning to Living Nexus performs one best-effort resume of the existing source and position; it never reloads or rewrites the queue. A real control-driven Pause followed by a background-tab round trip remained paused, while a simulated hidden browser pause resumed successfully. Focused contracts passed 6/6, TypeScript/build passed, full regression reached 597 passed / 1 skipped with only the established unrelated CinematicSplash marker failure, and refinement stayed at the existing 70/100 baseline.
- [x] Diagnose the reported production `livingnexus.manus.space` Cloudflare Error 1101 Worker exception (Ray ID `a38a111ffce36b0a`, reported 2026-09-09 23:45:29 UTC) using production logs, route responses, and deployment evidence before changing application behavior. Managed application logs showed no corresponding exception; all three public domains and critical public routes returned 200 after the report, and the primary host rendered in a browser. The edge-layer failure was transient and not reproducible; the evidence record is in `docs/lnls/engineering/PRODUCTION-WORKER-1101-INCIDENT-2026-09-09.md`.
- [x] Repair only the confirmed Worker/runtime cause and verify critical public routes without changing playback, Works, albums, WIDs, provenance, creator records, or publication authority unless evidence directly requires it. No source repair was warranted or performed because no application-level cause was evidenced. If it recurs with clean app logs, retain the Cloudflare Ray ID and escalate it as a managed edge/runtime incident rather than applying speculative app changes.
- [x] Add bounded auto-resume for an already listener-started stream after a recoverable temporary network interruption, with position recovery, a finite retry budget, no document reload or queue replacement, and no override of a deliberate listener pause. The singleton player now retries only the same current durable source after recoverable network errors or sustained stalls, preserves last known position, uses 750ms/2s/5s bounded backoff, avoids retrying browser-policy rejection, and leaves the existing Play control available after exhaustion. Successful resumed playback, explicit Pause, Work/queue changes, completion, natural buffering recovery, and cleanup all cancel retry state. Background stalls/errors now use the same bounded path. Local browser verification simulated offline recovery of the same source/position and confirmed an explicit Pause prevents further retries; focused contracts passed 6/6, TypeScript/build passed, and full regression reached 597 passed / 1 skipped with only the established unrelated CinematicSplash marker assertion failing.
- [x] Inventory only verified Living Nexus identity, Work, WID, provenance, attribution, AI-disclosure, and lineage facts needed for a separate NEX-ARK Bridge handoff.
- [x] Define mock-first Nexus Bridge provider interfaces, stable reference identifiers, errors, privacy/consent rules, and replacement criteria without inventing Living Nexus production endpoints.
- [x] Write a repository-ready NEX-ARK handoff brief defining separate-application boundaries, domain models, Connected Identity prototype flow, adapter contracts, and phased implementation plan.
- [x] Review the handoff for separation, canonical-authority, privacy, and implementation-readiness constraints; deliver it without changing NEX-ARK or Living Nexus runtime behavior. The standalone contract compiles under TypeScript strict mode; documentation diff hygiene and source-reference target checks passed. No NEX-ARK or Living Nexus runtime code, schema, data, WID, provenance, or external API behavior changed.
- [x] Save the Nexus Bridge specification and NEX-ARK repository handoff as a documentation-only checkpoint for delivery to the separate project.
- [x] Save the verified TopBar search and Explore Columns control removal as a checkpoint and hold its public-web verification for the project Publish action.
- [x] Audit existing Registry readers, public API routes, API-key/key-validation records, authorization seams, and WID/provenance custody before proposing any database or Registry API access point. The audit confirmed a useful but fragmented foundation: canonical read-only WID policy in `workRoute`, a broader mixed `/api/v1` surface, bcrypt-hashed creator keys with creation/list/revoke, and no scopes/expiry/integration identity/access audit. Direct database access remains prohibited.
- [x] Define a dedicated versioned Registry API boundary with public versus partner/private resources, hashed scoped keys, one-time display, expiry, revocation, rate limits, attribution, and append-only access audit—without direct database exposure. The proposed initial surface is read-only `/api/registry/v1`; key scopes do not override public visibility, creator ownership, or WID/testimony custody.
- [x] Produce a Registry API/key-issuance ADR and bounded implementation plan, including schema/migration, security, rollout, test, operational, and rollback requirements; await explicit approval before code or key issuance. See `docs/lnls/architecture/ADR-REGISTRY-API-KEY-ISSUANCE.md`; no Registry code, schema, API key, credential, WID, provenance, or database record has been changed or issued.
- [x] Reconcile the Keeper-supplied Registry API R1 issuance specification with `ADR-REGISTRY-API-KEY-ISSUANCE.md`: administrative client creation; server-side maximum scopes/quota; one-time high-entropy credential display; hash/prefix/client/scopes/expiry-only persistence; secret-store placement; non-secret issuance/audit evidence; rotation overlap; immediate revoke; privacy-minimized request audit; and the separate delegated Living Nexus user authorization requirement for private queue, library, testimony, or creator-corpus access. The ADR now locks the canonical external base URL, exact seven R1 read scopes, version-2 HMAC-with-pepper credential shape, first-party Nexus secret-store rule, and delegated-private exclusion.
- [x] Produce the consolidated implementation-ready R1 route, schema, enforcement, migration, acceptance-test, and rollback plan without issuing a key or changing any production schema, database record, WID, provenance record, or Registry authority. `ADR-REGISTRY-API-KEY-ISSUANCE.md` now carries exact routes, typed envelope, v2 schema fields, enforcement order, migration/deployment gates, acceptance criteria, and layered rollback. No code, schema, credential, database, WID, provenance, or Registry state changed.
- [x] Implement R1a additively: administrative API clients; v2 high-entropy one-time credentials; fixed scope subsets; expiry, rotation, immediate revocation, server-owned quota, non-content access audit, and key validation. Preserve version-1 Work-registration-key behavior while granting it no Registry authority. The admin-only tRPC control plane creates/revokes server-governed clients, creates/rotates/revokes v2 `lnr_{test|live}_...` credentials, and never returns stored digests; legacy registration keys retain their original isolated flow and no caller-selected tier survives.
- [x] Apply and verify the additive R1a schema migration without issuing a test or live Nexus credential and without changing Works, WIDs, provenance, testimony, creator records, or registration authority. Migration `0138_registry_api_r1a_credentials.sql` added the client/credential/scope/quota/audit tables; pre-migration absence and post-migration empty-state were verified. Controlled acceptance exercised TEST records only and revoked every resulting client/credential; database verification shows only TEST / REVOKED credentials and no active or LIVE Registry credential. WID, Work, testimony, provenance, creator, and registration data were not changed.
- [x] Implement R1b: read-only `/registry/v1` capability, search, autocomplete, creator, Work, WID, public provenance, and public-permission routes through governed projections; add the server-only Nexus adapter contract without adding a browser secret. R1b is mounted at `/api/registry/v1`, emits a single typed `registry.v1` envelope, projects only Published + `isPublic` + registered Works, excludes raw ORM IDs, storage, lyrics, testimony, creator corpus, and raw provenance payloads, and has an adapter that reads only server environment variables.
- [x] Validate the full R1 security/custody matrix: key secrecy, scope/expiry/revocation/rotation/quota enforcement, anti-enumeration, non-content audit, public/private/write denials, visibility suppression, no direct database access, and server-only Nexus adapter isolation. Focused R1 and legacy-key suites passed 29/29; the controlled test-only acceptance harness passed authorization, tampered/unknown-key indistinguishability, rotation overlap, immediate revocation, 429 quota, public-work health, private/write denial, audit hygiene, and automatic test credential revocation. TypeScript and production builds passed. Full regression reached 608 passed / 1 skipped with only the established unrelated CinematicSplash 49.8%/50.2% marker assertion failing. No live Nexus credential has been issued.
- [ ] Confirm the Nexus staging endpoint, connected Nexus repository/service path, secret-store ownership, offline-fixture adapter seam, and the approved short TEST credential lifetime/quota before controlled integration.
- [ ] Publish and verify Registry API R1 in staging only: migration state, `/api/registry/v1/health`, capabilities, and read-only public routes; leave production data and LIVE credentials untouched.
- [ ] Create one finite TEST first-party Nexus client with server-created approved read scopes and strict server-owned rate/quota ceilings; place the one-time credential directly into the Nexus staging server secret store, never browser/client source/logs.
- [ ] Replace Nexus Chat’s local Registry fixture only through its existing server-side adapter, retaining an explicit offline/test fallback and preserving `UNSPECIFIED` as distinct from permission to use a record.
- [ ] Execute the controlled witness-path acceptance matrix, including WID/context/permission receipt evidence, permitted-record-only question handling, forbidden scope, guessed private ID, expiry, rate limit, and immediate revocation checks.
- [ ] Revoke the TEST credential; prove subsequent calls fail; inspect sanitized access audit; record exact build, migration, test, client/credential lifecycle, receipt, and provenance evidence without exposing raw secrets or protected material.
- [x] Reconcile the supplied `ln-ai-chat` local harness with Registry R1 checkpoint `f01efb4e` source identity; treat the harness as a test artifact, never as deployed credential authority. The supplied archive matches Nexus harness source `184f3eb03a1378c6b0c936f6080c8e502517195e`; Registry R1 is confirmed as `f01efb4e92fe4686a332de8576f3a0eb9e177e90` in `Whisperer217/living-nexus_provenance`.
- [x] Create a separate deployable Nexus backend/staging foundation outside `Whisperer217/nex-ark-prototype`, with server-only Registry/provider seams, LN session integration, durable conversation/context/execution/audit storage, strict origin/rate/size/ownership checks, explicit staging configuration, and health/readiness endpoints. The separate private repository is `Whisperer217/nexus-service`; deployed source commit `8a4742806edd4ee88f0fa23ba16590d2ec89b9ff` serves at `https://nexus-staging.35.196.142.88.nip.io`. It uses durable SQLite migration `nexus-staging-0001`, root-owned server config, loopback-only Node service, TLS Caddy proxy, authenticated OIDC/JWKS seam, encrypted message storage, ownership checks, per-subject/IP rate buckets, request-size limits, content-free audit, and server-only Registry adapter configuration.
- [x] Preserve Nexus custody invariants: public is distinct from AI-permitted; playing is distinct from AI context; upload is distinct from registration; AI inference is distinct from creator testimony; Nexus is distinct from a model provider; and UNSPECIFIED never grants permission. Staging tests cover verified-session conversation ownership and explicit denial when the Registry permission state is `UNSPECIFIED`; the legacy harness contract retains all 25 prior custody tests.
- [x] Before any Registry credential issuance, deliver staging URL, deployed commit, migration result, server-side secret-store destination, health/readiness result, and redacted Registry adapter configuration; await explicit TEST credential authorization. Public `/healthz` returns 200 over TLS; `/readyz` intentionally returns 503 with `lnSessionVerification=missing` and `registryAdapter=absent`; protected routes fail closed with `NEXUS_LN_AUTH_NOT_CONFIGURED`. The secret file is `/etc/nexus-staging/nexus-staging.env` with mode `0640 root:ubuntu`; Registry key is absent. No TEST or LIVE Registry credential was issued.
- [x] Repair the Registry R1 admin-router module export regression surfaced by development runtime reload during Nexus staging verification. Restored the credential-service query-helper import surface, restarted the server, and revalidated TypeScript plus Registry R1a/R1b/pepper contracts (11/11); the stale `revokeRegistryApiClient` import error no longer appears in current startup logs.
- [x] Inspect and configure only noncredential Living Nexus staging identity metadata—JWKS URL, issuer, and audience—and the Registry staging base URL for Nexus; do not create or place a Registry credential. Nexus now holds the nonsecret JWKS, issuer, audience, and Registry base URL; `REGISTRY_API_KEY` remains absent.
- [x] Make Nexus readiness require database, encryption, LN assertion verification, and configured Registry adapter for witness-capable operation; preserve fail-closed protected routes until every noncredential boundary is verified. Readiness now returns 503 until the Registry adapter has a future server-only TEST credential; uncredentialed protected routes return 401.
- [x] Verify externally reachable health, configured readiness, signed assertion rejection/acceptance behavior, Registry adapter configuration without a key, and no browser/provider/Registry secret exposure; then stop and request separate TEST credential authorization. After manual publish, Nexus staging reached Living Nexus identity metadata (200), public Ed25519 JWKS (200), Registry health with credential digest ready (200), and public capabilities (200) with private/registration/policy features explicitly `notConnected` and delegated authorization required. Nexus external health is 200; it remains intentionally 503/not-ready with `registryAdapter=absent`, and protected Nexus calls return 401 absent a Living Nexus assertion. No Registry/provider secret appears in public responses or browser code.
- [ ] Create exactly one staging TEST Nexus Registry client/credential with the approved seven read scopes, a 60-minute expiry, 60 requests/hour quota, no rotation predecessor, and no LIVE credential; place the one-time secret only in `/etc/nexus-staging/nexus-staging.env` with no browser, log, Git, or receipt disclosure.
- [ ] Run the authorized finite TEST witness-path acceptance: Registry reads and allowed context attachment only; verify `UNSPECIFIED`/private/guessed-ID/write scope denials, no implicit AI context from playback, non-content audit, expiry-ready lifecycle, quota handling, and stable execution receipt fields without provider inference.
- [ ] Revoke the finite TEST credential before completion; prove Nexus readiness returns 503 and Registry calls fail thereafter; record exact build, migration, test, credential lifecycle, and sanitized audit evidence without creating a LIVE credential.
- [x] Repair the Registry R1 permission-envelope mismatch discovered during TEST acceptance: map explicit existing `aiConsent` values into a stable `aiPermission.state` contract consumed by Nexus, while retaining `UNSPECIFIED` for unknown/absent permission and never inferring AI permission from public visibility or playback. `permitted` and `permitted_attribution` map to `ALLOW` (with attribution requirement preserved); `prohibited` maps to `DENY`; null/unknown maps to `UNSPECIFIED`. Nexus consumes the stable field while retaining legacy explicit-consent fallback only during staged rollout. Focused mapping/Registry/Nexus tests passed 7/7, production build passed, and full regression reached 612 passed / 1 skipped with only the established unrelated CinematicSplash 49.8%/50.2% marker assertion failing.
- [ ] Diagnose and correct the discovered Registry credential validation mismatch: the finite TEST credential issued by the local development service was rejected by the published Registry service despite an active database record, indicating distinct server-held pepper/runtime boundaries. Revoke the unusable TEST credential, remove it from Nexus staging, preserve fail-closed readiness, and issue any replacement only from the deployed-compatible administrative boundary.
- [x] Revoke the unusable local finite TEST credential and restore the no-credential fail-closed boundary. Client/credential `60001` are both `REVOKED`; its key was removed from `/etc/nexus-staging/nexus-staging.env`; Nexus readiness again returns expected `503` with `registryAdapter=absent`. The TEST witness attempt created no Registry access audit event and did not attach context or execute a witness receipt because the published Registry rejected the development-issued credential as invalid.
- [ ] Rotate `LN_REGISTRY_API_PEPPER` to one newly generated secure single-line value through project Settings, publish/restart Living Nexus, and prove the same test-only credential validates at both the local issuer and published Registry runtime before asking again to issue a replacement TEST credential. No existing LIVE credential requires continuity.
- [x] Create one separately re-authorized replacement Nexus staging TEST Registry client/credential from the rotated published-compatible pepper with all seven approved read scopes, 60-minute expiry, 60 requests/hour quota, server-only secret placement, and no LIVE credential. Client/credential `90001` were TEST-only; the one-time secret resided only in the root-owned Nexus staging secret file and was removed after acceptance.
- [x] Prove immediate published Registry validation before continuing the replacement witness path; then run permitted context, `UNSPECIFIED`/private/guessed-ID/write denial, rate-limit, audit, and revocation checks before removing the key from Nexus staging. Published authenticated capabilities returned 200. The real WID permission endpoint returned `ALLOW`; an explicit attached context produced a `202` witness receipt with `PROVIDER_NOT_CONFIGURED`. Valid assertion with no attachment returned `403 NEXUS_CONTEXT_REQUIRED`; guessed WID returned `404 NOT_FOUND`; private route returned `403 DELEGATED_AUTHORIZATION_REQUIRED`; write returned `405 METHOD_NOT_ALLOWED`; finite quota ended at `429 QUOTA_EXCEEDED`.
- [x] Close the replacement finite TEST credential after recording its functional published Registry authentication and the evidence that no existing Published + public + registered Work has explicit `permitted` or `permitted_attribution` consent. Do not mutate creator consent or create a synthetic Work merely to force the positive witness path; record the positive-path acceptance as blocked pending a creator’s deliberate consent on an existing registered Work. Superseded by the authoritative governed Registry permission response: `WID-MUS-C1FCC012-70E4B7CC` returned explicit `ALLOW`, so the positive path was completed without changing creator consent or creating data.
- [x] Complete the permitted witness-path acceptance using the published Registry’s explicit `ALLOW` response for `WID-MUS-C1FCC012-70E4B7CC`; the database hash join was not a valid candidate-selection projection, but the governed Registry permission endpoint is authoritative for the service boundary. A short-lived signed Living Nexus assertion created a conversation, explicitly attached the allowed WID, and produced a persisted permitted witness receipt. The provider remained deliberately absent; no external inference or testimony transfer occurred. Client/credential `90001` were revoked client-wide; the secret was removed from Nexus; readiness reverted to expected `503 registryAdapter=absent`. Sanitized Registry audit contains issued/revoked lifecycle events, 59 allowed reads, and one delegated-private denial; it stores hashes for IP/user agent and no raw query/content field.
- [x] Confirm DNS ownership and create a branded `ai.livingnexus.org` hostname for the separate Nexus staging service without exposing Registry/provider endpoints or port 4318 directly. The A record resolves to the staging host, Caddy terminates branded TLS and proxies only to loopback `127.0.0.1:4318`, port 4318 remains private, recovery hostname remains retained, and strict browser-origin policy accepts configured Living Nexus/Nexus origins while rejecting untrusted origins.
- [ ] Bind the branded hostname through TLS Caddy reverse proxy with strict Living Nexus origin controls and verify that only the minimal signed-in Nexus chat surface, health/readiness, and no-secret server boundaries are reachable.
- [x] Design and implement a minimal authenticated Nexus chat entry surface at the branded hostname, retaining Registry as server-only, preserving offline fixture fallback, and deferring external model-provider calls until separately approved. The separate `Whisperer217/nexus-service` staged entry at `https://ai.livingnexus.org/chat` is deployed at `5ff1fd5`; it describes the permission boundary, has no provider configured, and exposes no Registry/provider credential to browser code.
- [x] Replace the failed cross-site cookie assertion fetch with a nonce-bound first-party Living Nexus signed-launch popup that transfers only a short-lived assertion to the exact branded Nexus origin through `postMessage`, never exposing a Living Nexus session, Registry credential, or provider credential to the Nexus browser entry. Cross-site cookies are no longer required; the target validates origin and a 24-byte nonce before accepting an assertion, while Living Nexus refuses untrusted/missing-nonce launch targets.
- [x] Verify the branded Nexus chat can connect with an authenticated Living Nexus browser session through the signed-launch handoff while rejecting invalid return origins, nonce mismatches, third-party messages, and unconfigured provider use. After publication and first-party `www.livingnexus.org` sign-in, the real `ai.livingnexus.org/chat` browser tab displayed the connected five-minute tab-local secure-session state. Anonymous launch is `401 NEXUS_ASSERTION_UNAUTHORIZED`; browser entry scan found no Registry/provider/private-key markers; Nexus remains credential-free with readiness `503` because its Registry adapter is absent; its provider remains explicitly unconfigured.
- [x] Repair the branded signed-launch host mismatch: the published assertion metadata targets `livingnexus.manus.space`, but the authenticated browser session is held at `www.livingnexus.org`; align the public issuer/JWKS/launch origin and Nexus verifier to the same custom first-party host so a signed popup can access the existing user session without widening cookie or credential exposure. Living Nexus now defaults the assertion issuer to `https://www.livingnexus.org`; Nexus launch link/client CSP and cloud JWKS/issuer configuration match that host. Focused assertion and Nexus staging tests passed 8/8; production build passed. The complete regression reported only the established CinematicSplash marker failure plus an unrelated Sonner/document test-harness loading failure under constrained worker mode.
- [x] Prepare a Gemini staging-provider architecture plan for the separate Nexus service only: server-held provider key, fixed initial model policy, model/cost/hash receipt fields, request/response safety and size controls, explicit Registry-context boundary, offline fixture fallback, service-level quota/cost ceiling, monitoring, test matrix, rollout, and rollback. The approval-ready plan and official-source research are recorded separately in `nexus-service/GEMINI_STAGING_PROVIDER_PLAN.md` and `nexus-service/GEMINI_STAGING_PROVIDER_RESEARCH.md`. No Gemini key was requested/stored, no model was invoked, no browser provider capability was exposed, and the current keyless Registry/Nexus state remains unchanged.
- [x] Extend the staged Nexus provider plan so one branded chat interface can offer text inference and an explicit image-generation action through the same server-only Gemini provider family, while retaining separate capability gates, prompt safety, generated-asset custody, content labels, cost/receipt fields, and no-likeness/no-testimony substitution boundaries. The unified approval-ready architecture is now in `nexus-service/GEMINI_STAGING_PROVIDER_PLAN.md`, with updated official Gemini image/model/safety/key research in `nexus-service/GEMINI_STAGING_PROVIDER_RESEARCH.md`. No provider key, image invocation, browser secret, generated asset, or automatic image generation is authorized by this planning slice.
- [x] Replace the unsupported direct reuse of Living Nexus HS256 session cookies with a short-lived Ed25519 Nexus assertion minted only from an authenticated Living Nexus session; publish its public JWKS, enforce an explicit issuer/audience, and never expose the session or assertion private keys. The new assertion has a 5-minute TTL, issuer `https://livingnexus.manus.space`, audience `nexus-staging`, and public key ID `ln-nexus-staging-2026-09`; the signed session cookie and private assertion key stay server-only.
- [x] Validate the supplied `LN_NEXUS_ASSERTION_PRIVATE_KEY` through a server-only endpoint/test, verify JWKS does not disclose private material, and prove Nexus rejects wrong issuer, audience, signature, expiry, or unpermitted context assertion. Focused assertion contracts pass 3/3; JWKS derives through Node crypto without exporting the non-extractable signing key; Nexus staging contracts reject wrong issuer/audience and deny `UNSPECIFIED` context.
- [x] Accept a one-line Base64 encoding of the existing server-only PKCS#8 Ed25519 PEM when multiline secret storage cannot preserve PEM boundaries; decode only in memory, retain direct PEM compatibility, and never log, return, or commit either representation. Direct PEM and one-line Base64 decode support are covered by unit test; the private representation is absent from responses, JWKS, and source.
- [x] Produce a read-only decommission and re-establishment map for the Living Nexus Provenance Nexus Avatar surface: its routes, UI entry points, PNA/Guide/avatar data models, ownership and entitlement records, WID/provenance dependencies, public redirects, safe archival/redirect options, and successor boundaries. The completed record is `docs/lnls/architecture/ADR-PNA-RETIREMENT-AND-AVATAR-SUCCESSOR.md`; no route, record, entitlement, avatar, WID, or provenance state was altered. Retirement remains blocked pending Keeper approval of a migration/transition plan.
- [x] Draft the technical specification for the Living Nexus Ingestion Commission and worker pipeline, defining deterministic stages, worker/job contracts, creator confirmation boundaries, durable receipts, retries, failure handling, hosting options, security limits, and zero-authority automation rules. The final architecture-only contract is in `docs/lnls/architecture/LN-CORE-INGESTION-NEXUS-ADAPTER-PNA-PRESERVATION-SPEC.md`; no worker was activated and no upload/WID/publication behavior changed.
- [x] Draft the exact external Nexus AI Registry adapter contract, including server-only authentication, scope/permission enforcement, explicit context attachment, typed request/response envelopes, timeout/rate/audit rules, failure modes, receipt requirements, and browser/database isolation. The contract locks the seven R1 read scopes, context resolution and fail-closed rules, finite server-only credential posture, and no direct database/browser access; no credential was issued and no provider was invoked.
- [x] Integrate the PNA preservation/archive strategy with the Ingestion Commission and Nexus boundary, defining what may never flow automatically into workers, Registry context, AI inference, or a future avatar successor. The specification retains PNA as an archive-only source with no automatic path into any new system; no PNA route was retired and no successor was created.
- [x] Implement approved I1 — Living Nexus Core Ingestion Commission foundation: additive commission/job/inspection-receipt schema and migration; protected creator-scoped start/attach/cancel/get/list procedures; storage-verified deterministic audio inspection with receipt/hash, durable optimistic job claiming, bounded retries, lease recovery, and a scheduler-only callback endpoint. The deployment schedule is deliberately not created or activated. Model/provider, PNA, Quiver, Keeper, Guide, marketplace/avatar, existing upload, Draft registration, WID/provenance, publication, and schedule activation remained out of scope.
- [ ] Await Doc’s separate approval before deploying/activating any Core Ingestion worker schedule, adding the I2 creator review/private-Draft confirmation flow, integrating a model/provider, changing existing registration/WID/publication behavior, or beginning a PNA archive/successor implementation.
