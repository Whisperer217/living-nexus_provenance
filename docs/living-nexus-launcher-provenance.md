# Living Nexus launcher — implementation provenance

- **Origin / authorial input:** Jacob Ross's 2026-09-12 launcher and favorites request; the symbolic vocabulary ◆, @, ♪, ◇, ⛓, ✦, ▣, ⇲, ◌, ▤, ⌕, ⚙ comes from that request.
- **Source checkout:** `Whisperer217/living-nexus_provenance` main at `58049283dddad5c12d0fd3a71c8ce1e1418e9f69`.
- **Existing architecture preserved:** `MainLayout` owns the shared site shell; `TopBar` owns desktop chrome; the mobile header remains in `MainLayout`; `PlayerProvider` and `WSPProvider` remain the only playback authorities. No page-local audio engine was introduced.
- **New derivative:** `LivingNexusLauncher` renders original line-art SVGs and a 12-place grid. Favorites are a browser-local preference keyed by the signed-in LN user ID or `guest`; they do not modify server identity or registry records.
- **Route policy:** connected tiles use declared site routes. Creators and Works use the canonical Explore view query; Registry uses `/witness-registry`; Provenance links to the WID specification; Store points to the existing avatar marketplace; Community opens the existing LN Discord in a separate tab. Player opens the existing WSP only when a track is loaded. Nexus AI and Messages remain visibly unconnected until their independent services are ready.
- **Boundary:** an unavailable tile has no guessed route or implied permission. Opening the launcher, changing favorites, and opening the player do not attach a Work to Nexus AI context.
- **Verification:** TypeScript check and production build passed; focused launcher route and layout contracts passed (3/3); local desktop, 390px, and 320px browser inspections confirmed visual presentation, creator-directory navigation, favorites persistence, and Escape dismissal. The repository-wide local suite returned 611 passed / 24 failed / 1 skipped. Failures include unavailable local database/secrets, Windows path/line-ending assumptions, and the previously reported CinematicSplash marker assertion; none involve the focused launcher contracts. The local Vite preview has no live registry or LN session, so authenticated destination behavior and production rollout remain unverified. Do not describe this as 100% site-verified.
- **Migration:** none.
- **Tablet and accessibility correction (2026-09-12):** the inherited desktop `TopBar` visibility began at the `md` breakpoint while the existing mobile header remained visible until `lg`, causing both headers and both launcher instances to appear at 768–1023px. The desktop bar now begins at `lg`, matching `MainLayout` and `LeftRail`. Each launcher instance uses a unique panel ID so its trigger references the correct dialog. No audio authority or route changed. TypeScript check, production build, and focused layout/launcher contracts (4/4) passed after this correction.
- **Mobile navigation connection (2026-09-12):** `MainLayout` already exposed a top-left disclosure button with `aria-controls="mobile-nav-rail"`, but the existing off-canvas `LeftRail` had no matching ID and its hidden links remained focusable. The rail now carries that ID and becomes inert while closed. This preserves the left rail as navigation authority and the right grid as a separate launcher. TypeScript check passed; no routing or playback change.
