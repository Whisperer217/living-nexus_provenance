---
name: WID Generation for Updated Files
use_when: When a user updates an existing file on the platform that already has a WID.
---

When a user updates a file that already has a Witness ID (WID), a **new WID** should be generated for the new file. The old WID should remain in the archive, pointing to the original file. This process consumes an additional slot.
