---
name: Mobile Content Display: Text Overflow Management
use_when: When displaying text blocks within slides or similar content areas on small screens, especially when text length varies.
---

When displaying text blocks on small screens, if any text block is significantly taller than others, the text should be capped at a maximum of two lines using `line-clamp-2` to prevent overflow within a fixed container.
